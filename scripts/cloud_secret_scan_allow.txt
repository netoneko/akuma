# Literals that look like an AWS account id but are not. One per line.
# Only the bare 12-digit heuristic in scripts/check_no_cloud_secrets.sh reads this
# file; an access key or credential assignment is never allowlistable.

# Empty. The one historical entry -- a decimal Akuma box id in
# docs/archive/RUMP_SYSPROXY.md -- went away when that doc moved to the hex form
# `ps` actually prints. Prefer doing that to allowlisting: hex box ids cannot
# collide with the 12-digit account-id shape at all.
