[workspace]
resolver = "2"
members = [
    "libakuma",
    "akuma-std",
    "echo2",
    "elftest",
    "hello",
    "herd",
    "httpd",
    "stackstress",
    "stdcheck",
    "wget",
    "boa"
]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
opt-level = "s"

