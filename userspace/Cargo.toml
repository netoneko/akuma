[workspace]
resolver = "2"
members = [
    "libakuma",
    "echo2",
    "elftest",
    "hello",
    "herd",
    "httpd",
    "meow",
    "quickjs",
    "sqld",
    "stackstress",
    "stdcheck",
    "wget",
]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
opt-level = "s"

