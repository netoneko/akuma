[package]
name = "box"
version = "0.1.0"
edition = "2021"

[dependencies]
libakuma = { path = "../libakuma", features = ["chunked-allocator"] }
libakuma-tls = { path = "../libakuma-tls" }

[[bin]]
name = "box"
path = "src/main.rs"
