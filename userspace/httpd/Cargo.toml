[package]
name = "httpd"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "httpd"
path = "src/main.rs"

[features]
default = ["cgi-log"]
cgi-log = []

[dependencies]
libakuma = { path = "../libakuma" }
