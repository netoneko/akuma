
17. **CodeDiff** - Compare the content of two files and generate a unified diff.
   Args: `{"file1": "path/to/file1", "file2": "path/to/file2"}`
   Note: Uses the diff crate for generating unified diffs. Requires asynchronous file reading.