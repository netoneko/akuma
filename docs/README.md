# Akuma OS Documentation

This is the front door for all Akuma documentation. Use the tables below to
find what you need.

## How these docs are organized

```
runbooks/     Action-first. "Do X, expect to see Y." For debugging and building.
reference/    Current-state architecture and invariants. No history.
userspace/    Thin pointers to per-binary docs (kept co-located with source).
archive/      Every historical doc, moved verbatim. Linked from new docs, never rewritten.
```

Authoring conventions for each kind live in that section's own README:
[`runbooks/README.md`](runbooks/README.md) and
[`reference/README.md`](reference/README.md).

## Stability grades

Each reference doc carries a grade based on git churn history (608 commits
across 179 archived docs, Dec 2025 – Jul 2026):

- **A — Stable:** dormant or stabilized 2+ months. Safe to trust.
- **B — Watch:** low/med churn, recent but not chronic. Verify behaviour.
- **C — Active risk:** high churn, touched in Jun/Jul. Expect surprises.

The fire windows were late Feb–Mar 2026 (syscall-gap crisis) and Jun 2026
(memory + signal crisis). April/May were the cool-down tails.

## Symptom matrix - "I see X, what do I read?"

| Symptom | Start here |
|---|---|
| nginx (or any epoll server) serves at **~17 ms/request** on some boots and ~0.1 ms on others, 0 failed requests, tight within a boot | A **lost epoll wakeup** rescued by the 10 ms `backstop_us` — `ab -k` halves it, and Akuma's own blocking `httpd` on the same boot is 16x faster. **Do not tune the backstop**: [`archive/NGINX_LOST_WAKEUP.md`](archive/NGINX_LOST_WAKEUP.md) |
| **SMP=1**: whole kernel freezes (ticks stop, no panic, QEMU ~3 % CPU) when starting a **second server** from an SSH session while a listener is already bound — SMP=4 unaffected (verified at same RAM) | **FIXED** (`2439994b`): waiter parked in `idle_halt` (yield-less `blocking_relax_net`) monopolized the only core preempt-disabled; ticks ran the wake-pass but the switch was suppressed and no running thread remained to raise a voluntary reschedule. Fix keeps non-idle halters preemptible; verified by A/B vs parent + `nettest parkprobe`. Firecracker sibling likely covered, untested there: [`archive/SECOND_LISTENER_SMP1_FREEZE.md`](archive/SECOND_LISTENER_SMP1_FREEZE.md) |
| VM won't boot / hangs early | [`runbooks/debug-boot-hang.md`](runbooks/debug-boot-hang.md) |
| SSH unreachable or slow to connect | [`runbooks/debug-devbox.md`](runbooks/debug-devbox.md) (devbox) / [`runbooks/debug-ssh-latency.md`](runbooks/debug-ssh-latency.md) |
| `ssh -p 2222` refused / `Connection reset by peer`, but the box booted fine and herd logged `Started sshd` | Disk populated before 2026-08-10: `sshd.conf` still says `--port 23`, so the box answers on **`-p 2323`**. Re-overlay `bootstrap/etc/` to move it to 22. [`archive/BUILTIN_SSH_REMOVAL.md`](archive/BUILTIN_SSH_REMOVAL.md) |
| `head`/`wc`/`sed`/`ps` "not found" over SSH although `/bin/busybox` and 300+ symlinks exist | `populate_disk.sh` linked applets to the populate container's path (`-> /mnt/disk/bin/busybox`). Fixed; repopulate the disk. [`archive/BUILTIN_SSH_REMOVAL.md`](archive/BUILTIN_SSH_REMOVAL.md) |
| `/bin/sh: can't fork: Out of memory` after a few SSH sessions on a 4–4.5 MB box, no `[OOM]` line (contrast: the execve-leak class always prints `[OOM]`) | **OPEN.** File-page dedup cache is capped at `RAM/8` (144 pages at 4.5 MB) but busybox needs 273, so concurrent instances stop sharing text. Tell: `[FPCACHE]` with `evict` ≈ `misses` and `entries` pinned at cap. [`archive/FPCACHE_UNDERSIZED_AT_LOW_RAM.md`](archive/FPCACHE_UNDERSIZED_AT_LOW_RAM.md) |
| `kex_exchange_identification: read: Connection reset by peer` when many clients connect **at once** (a lower number connecting one at a time is fine) | The listener has no SYN queue — it *is* a fixed pool of pre-listening sockets, `MAX_BACKLOG` in [`crates/akuma-net/src/socket.rs`]. 32 with the default `many-sessions` feature, **8** without it (`extreme-size`, `size`, rump `devbox`, any `--no-default-features` build). Arrivals past that get a RST no matter how fast the server accepts: [`userspace/sshd/docs/PROCESS_PER_SESSION.md`](../userspace/sshd/docs/PROCESS_PER_SESSION.md) |
| A process ignores `SIGTERM` (or any handled signal) — `kill` returns 0, the process keeps running, only `kill -9` works — and it is a **forked child that never called `exec`** (a master/worker daemon: nginx, and anything else that installs handlers before forking) | `fork`/`vfork` gave the child a fresh, all-`Default` disposition table instead of the parent's, silently un-installing every handler. **FIXED 2026-08-20** (`SharedSignalTable::clone_for_fork`). Two symptoms, one cause: a `Default` disposition never breaks a blocking syscall out with `EINTR`, and when the syscall does return the default action *terminates* instead of running the handler. Invisible for `fork`+`exec`, since `exec` resets handlers anyway. [`archive/FORK_LOSES_SIGNAL_HANDLERS.md`](archive/FORK_LOSES_SIGNAL_HANDLERS.md) |
| A port that was serving fine stops answering **permanently** after sustained connection churn — every new connection gets `Connection reset by peer` — and restarting the server fixes it instantly | Not the server. Akuma's listener is a **pool** of pre-`listen()`ed smoltcp sockets, and a handle reset before anyone accepted it went to `Closed` and was never returned to `Listen`, so the pool eroded to zero. **FIXED 2026-08-20** (`listener_refresh`). The restart "fixing" it is the misleading part — a restart rebuilds the pool. Read the new `BACKLOG` column of `/proc/net/tcp` (`listening/pending/dead`): `0/0/32` is a dead listener. Probe: [`scripts/probes/listener_backlog_churn.py`](../scripts/probes/listener_backlog_churn.py). [`archive/NGINX_MISSING_SYSCALLS.md`](archive/NGINX_MISSING_SYSCALLS.md) Issue E |
| A blocking `read()` on an accepted socket never returns, and the server (single-threaded) never gets back to `accept()` | The peer **reset** the connection: smoltcp puts it in `Closed`, which is also where a never-connected socket sits, so the readiness predicate called it "nothing yet" and waited forever. **FIXED 2026-08-20** — `KernelSocket::was_connected` supplies the missing history and `recv` now returns `ECONNRESET`. [`archive/NGINX_MISSING_SYSCALLS.md`](archive/NGINX_MISSING_SYSCALLS.md) Issue E |
| `SCM_RIGHTS` fd passing does nothing (`recvmsg` returns no ancillary data), or `SO_PEERCRED` reports **uid 0** for every peer | **Both are known gaps, not bugs.** Ancillary data is unimplemented (Phase 4); `uid`/`gid` are 0 because this kernel has no per-process uid, so do not authorise on that field — `pid` is real. Everything else in AF_UNIX works: bind/listen/accept/connect on paths and abstract names, `SOCK_DGRAM`, `SOCK_SEQPACKET` framing, `S_IFSOCK` nodes. Probe it with `nettest-unix all` (13 modes, diffed against a Linux control arm). [`archive/UNIX_SOCKET_IMPROVEMENTS.md`](archive/UNIX_SOCKET_IMPROVEMENTS.md) § 0 |
| `scripts/build_devbox.sh` (the rump-only devbox) fails with dozens of `unresolved module or unlinked crate \`smoltcp\`` errors | A `#[cfg(feature = "smoltcp")]` has drifted off the item it was meant to gate — an attribute attaches to the *next* item, and an inserted doc comment or `pub use` silently relocates it. Four had been lost this way. Fixed 2026-08-23; check `akuma-net`'s `lib.rs` first. [`archive/UNIX_SOCKET_IMPROVEMENTS.md`](archive/UNIX_SOCKET_IMPROVEMENTS.md) § 0 |
| An HTTP client hangs ~5 s per request against nginx while `curl`/a single `recv()` looks instant — the response arrives, the close does not | `shutdown(2)` was a `return 0` stub, so `SHUT_WR` never put a FIN on the wire. nginx's lingering close does `shutdown(SHUT_WR)` and then reads for up to `lingering_timeout` (default **5 s**), so a client reading to EOF saw nothing until that timer expired. **FIXED 2026-08-20**: 5007 ms → 2.6 ms per request. [`archive/NGINX_MISSING_SYSCALLS.md`](archive/NGINX_MISSING_SYSCALLS.md) Issue E |
| One bad SSH session kills *every* other session at once | Was structural: the cooperative sshd ran all sessions in one process and `panic = "abort"` is process-wide. **FIXED** by process-per-session (default 2026-08-10): [`userspace/sshd/docs/PROCESS_PER_SESSION.md`](../userspace/sshd/docs/PROCESS_PER_SESSION.md). If you are on a build with `SSHD_FORK_SESSIONS=0`, the old behaviour is expected |
| `[SSHD] fork failed`, or `/bin/sh: can't fork` once several SSH sessions are live on a low-RAM image | Each session is now a process (plus its shell) against a global `MAX_PROCESSES = 64`. Lower `max_sessions` in `sshd.conf`, or revert sshd to the cooperative executor: [`runbooks/build-extreme-size.md`](runbooks/build-extreme-size.md) |
| Cross-process coordination through `mmap(MAP_SHARED\|MAP_ANONYMOUS)` never syncs (a child's write is invisible to the parent) | `fork` CoW-copied the mapping instead of sharing it, so the flag was silently ignored. **FIXED 2026-08-15** (`MmapRegion::shared_anon` + `process::share_rw_range`). Probe: `bootstrap/bin/shmanon` — checks both the shared and private legs, calibrated on Linux/macOS; a FAIL is the kernel. [`archive/SELFHOST_ZERO_PAGE_HUNT.md`](archive/SELFHOST_ZERO_PAGE_HUNT.md) §9 |
| A multi-threaded CPU workload collapses when adding threads (llama.cpp `-t 1` fast, `-t 2+` 10-100x slower, all cores burning) while separate processes scale fine | Three causes, all **FIXED 2026-08-19**. Dominant: EL0 `CNTVCT_EL0`/`CNTFRQ_EL0` reads trapped (~1M pairs/s under decode, `CNTKCTL_EL1` never set) **and the trap emulation returned 0** — userspace timing ran on a frozen clock. Amplifiers: the context switch ran `tlbi vmalle1` even between threads of one address space (now skipped when TTBR0 unchanged), and the round-robin displaced working threads every 1 ms tick even with idle cores (now a bounded, **EL0-gated** displacement bypass). `tg` decode `-t 1/2/3`: 36/1.6/0.28 → 45/68/96 tok/s. Watch `[EXCC]` for a returning `e0.0x18` bucket. `-t 4` still open — but **not** for the reason it looks like: simulation (`cargo run -p akuma-scheduler`) shows oversubscription costs only ~10-15%, and reproducing the measured 14.6x needs a **2-3 ms futex wake latency**, which §2 independently measured. [`archive/CROSS_CORE_THREAD_COLLAPSE.md`](archive/CROSS_CORE_THREAD_COLLAPSE.md), [`archive/AKUMA_SCHEDULING_EXTRACTION.md`](archive/AKUMA_SCHEDULING_EXTRACTION.md) |
| I want to change a scheduler, cache or wake-rate policy and need to know if it will actually help | Model it before building it. `crates/akuma-scheduler` simulates placement and netpoll wake policies against a barrier-synchronous workload and ranks them; `crates/akuma-kacho` holds the observe/decide/hysteresis primitives every self-tuning policy here shares. Both are host-only and instant. [`archive/AKUMA_SCHEDULING_EXTRACTION.md`](archive/AKUMA_SCHEDULING_EXTRACTION.md) — including the two model bugs that produced confident wrong answers first |
| Self-host build slows down / `[FPCACHE]` shows `entries` pinned at cap with `evict` climbing | The over-cap eviction used to take whatever sat at the rotating cursor, so a full cache evicted **mapped** pages — freeing nothing and costing the next mapper a re-read. **FIXED 2026-08-15**: prefers an unmapped victim over a bounded scan. Read `evict_mapped/evict` — a high ratio means the cache is genuinely too small, not merely full. [`archive/FPCACHE_EVICTION_PREFERS_UNMAPPED.md`](archive/FPCACHE_EVICTION_PREFERS_UNMAPPED.md) |
| `rustc` ICEs mid-build with `decode error: Expected header tag [79, 68, 72, 84] but found [0, 0, 0, 0]`, or any page reading back as zeros mid-process | `[79,68,72,84]` is `"ODHT"`, a crate-metadata header — **never a compiler bug**. **PARTIALLY FIXED 2026-08-15**: root cause #1 was the prefault file fill running through an `Err(-1)` stub hook (`read_at_by_inode` since the akuma-exec extraction) with the result discarded — 856 silent zero pages per build; fixed, green builds 0/10 → 3/10. Root cause #2 is **also FIXED**: the inode lifecycle was not honored across `mmap` — `unlink` freed and reissued an inode while a live `LazySource::File` still named it (no reference held, unlike Linux), so a later fault read `i_size = 0` → `Ok(0)` → zero page, or another file's bytes after reuse → the garbage-byte modes (`STR_SENTINEL`, invalid `TyKind` tag `102`). The mapping now holds an `akuma_primitives::InodePin` and ext2 defers the free of a pinned inode. The ICE is **latent rather than proven gone** — treat any `[FILL-SHORT] got=Ok(0)` as a failure even on a green build. Guards in `[Mem]`: `[FILL-SHORT/prefault]` and `defer_leak=` must stay 0. Fix record: [`archive/ZERO_PAGE_ICE_FIX.md`](archive/ZERO_PAGE_ICE_FIX.md); full hunt: [`archive/SELFHOST_ZERO_PAGE_HUNT.md`](archive/SELFHOST_ZERO_PAGE_HUNT.md) §12-§15; [`archive/PREFAULT_INODE_STUB_ZERO_PAGES.md`](archive/PREFAULT_INODE_STUB_ZERO_PAGES.md) |
| A client on the box gets `Connection refused` from a server that is plainly listening (`/proc/net/tcp` shows `LISTEN`), while the *same* server answers fine from outside the VM | Two `crates/akuma-net/src/socket.rs` bugs, both **FIXED 2026-08-16**: a `connect` redial (the standard non-blocking `connect`→`EINPROGRESS`→poll→`connect` idiom, which hiredis/`redis-cli` uses) was handed to smoltcp, rejected as `InvalidState` and reported as ECONNREFUSED; and `bind(0.0.0.0:0)` on TCP stored port 0, so the next `connect` got `Unaddressable`. Connect errnos now distinguish ETIMEDOUT/EADDRNOTAVAIL/EISCONN/EALREADY. [`archive/REDIS_END_TO_END.md`](archive/REDIS_END_TO_END.md) §2 |
| Kernel panic `handle does not refer to a valid socket` (`smoltcp-0.12.0/src/iface/socket_set.rs:103`), often right after a `connect() = EINPROGRESS` | A non-blocking `connect()`'s handle was still in `net.connecting` when `close()` queued it for GC; the GC sweep freed the smoltcp slot and the very next `connecting` sweep in the same `poll()` call dereferenced it. **FIXED 2026-08-22** — `socket_close` now purges the `connecting` entry too. Deterministic, not a race: any non-blocking connect closed before it establishes hits it. [`archive/SMOLTCP_STALE_CONNECTING_HANDLE_PANIC.md`](archive/SMOLTCP_STALE_CONNECTING_HANDLE_PANIC.md) |
| `box run <official image>` prints `failed to spawn …/docker-entrypoint.sh`, or starts and then does nothing at all | Two different things. `failed to spawn` = `sys_spawn` had no `#!` handling (**FIXED 2026-08-16**, [`archive/DEVBOX_ISSUES.md`](archive/DEVBOX_ISSUES.md) Issue 14). Starts-then-silent = the entrypoint's `setpriv` privilege drop re-execs forever because there are no per-process credentials — **OPEN**, use `--entrypoint`: Issue 15 + [`runbooks/run-redis.md`](runbooks/run-redis.md) |
| `cat /proc/self/<anything>` says `No such file or directory` although `/proc/<pid>/<same file>` works | The VFS never chased the `self` symlink, so procfs received the literal string `self/status`. **FIXED 2026-08-16** (`resolve_self`, `src/vfs/proc.rs`). This is why `redis-server` could not start for four days, and why `setpriv` failed later: [`archive/REDIS_END_TO_END.md`](archive/REDIS_END_TO_END.md) §4 |
| A client reports `Protocol error, got "<c>" as reply type byte` (or any garbled stream) from a server on Akuma, but only for **large** replies — small ones are always fine | `sys_writev` did not stop at a short write, so anything bigger than the socket's 16 KB TX window came out spliced with the next iovec's bytes. **FIXED 2026-08-16**; the first wrong byte was `0x0d`, the `\r\n` of the following buffer. A/B and mechanism: [`archive/WRITEV_SHORT_WRITE_SPLICE.md`](archive/WRITEV_SHORT_WRITE_SPLICE.md) |
| Firecracker refuses the kernel with `InvalidImageMagicNumber`, or a microVM hangs with no console output | The image is not a raw ARM64 `Image`. Firecracker's `linux-loader` validates `0x644d5241` at offset 56 and nothing else. For Akuma: wrong link address — `_boot` must be `0x80300000`, which `overlays/devbox-firecracker/build.sh` asserts. For a distro kernel: modern `vmlinuz` is an **EFI zboot** PE wrapper (`MZ` + `zimg`) with the real Image compressed inside, so offset 56 is payload. [`runbooks/dump-firecracker-fdt.md`](runbooks/dump-firecracker-fdt.md) §2 |
| Under Firecracker at `SMP>1`, the boot core gets no timer interrupt and no scheduler tick, with no build error and no boot error | The GIC redistributor base is `0x3fff0000 - vcpu_count * 0x20000`, so a map pinned to one vCPU drives *another core's* frames. Confirmed by measurement at 1/2/4/8 vCPUs: [`reference/firecracker/fdt/`](reference/firecracker/fdt/). This cannot be fixed with a better literal — it needs the FDT-derived device map, [`../proposals/FIRECRACKER_PORT.md`](../proposals/FIRECRACKER_PORT.md) §5 |
| VM wedged, 100% CPU, unresponsive | [`runbooks/recover-wedged-vm.md`](runbooks/recover-wedged-vm.md) |
| A Firecracker/AWS guest boots, accepts ssh, and answers **every** command with `not found` — `ls`, `ps`, `uname` — while `/bin/busybox` itself exists and runs | Not an empty disk: the image has **no busybox applet symlinks**. `bootstrap/bin` ships only the busybox binary; `overlays/devbox/bootstrap.sh` step 4 creates the links inside the ext2 image, and `devbox-firecracker-aws`'s builder had no equivalent. **FIXED 2026-08-25** — it now takes the list from `busybox --list` (305 applets, via docker on a foreign-arch host). Do **not** use the static list in `bootstrap.sh` as a reference: it omits `ls`. [`../overlays/devbox-firecracker-aws/README.md`](../overlays/devbox-firecracker-aws/README.md) |
| In-guest `git clone https://…` fails `certificate is not yet valid (9)`, or `cargo fetch` cannot reach crates.io, although DNS resolves and the CA bundle is present | The guest is at **epoch 0**. Firecracker exposes no RTC on aarch64, Akuma's only wall-clock source is the QEMU virt PL031, and `clock_gettime(CLOCK_REALTIME)` degrades an unset clock to `0` rather than an error — so every certificate is future-dated. `date -s` cannot fix it either: `clock_settime` (nr 112) is unimplemented, which is also why busybox `ntpd`/`rdate` cannot work. `akuma_timer::set_utc_time_us` already exists, so the syscall is a small change. Workaround: vendor the crates. **OPEN** — [`archive/MISSING_NTP_SYSCALLS.md`](archive/MISSING_NTP_SYSCALLS.md) |
| VM wedges during an *idle* interactive TUI session (e.g. `meow`), ~200% CPU at SMP=2, `[WATCHDOG] disabled at src/syscall/term.rs:432` and `[BKL] stuck owner=2 waiter=1` in the log | **FIXED** — the blocking stdin-read loop called `disable_preemption()` *before* the blocking `term_state_lock`/`input_waker` acquire rather than only around the (brief) hold, so contention monopolized the core — and, since this syscall isn't BKL-opted-out, the global BKL — with no bound. Fixed via a per-attempt bounded acquire (`akuma_exec::sync::lock_bounded`). [`archive/TERM_POLL_INPUT_PREEMPTION_FIX.md`](archive/TERM_POLL_INPUT_PREEMPTION_FIX.md) + [`archive/DEVBOX_ISSUES.md`](archive/DEVBOX_ISSUES.md) Issue 2. Reference: [`reference/subsystems/syscalls/term.md`](reference/subsystems/syscalls/term.md) → "Blocking stdin read" |
| `[OOM] allocation of N bytes failed (heap …MB used) — killing process` repeating, sshd can't spawn `/bin/sh` — OR serial log frozen + 100% CPU with **no** `[OOM]` under exec-heavy load | Kernel-heap wall from the execve stack leak (FIXED) and its lock-abandonment hang class: [`reference/subsystems/thread-lifecycle.md`](reference/subsystems/thread-lifecycle.md) §4-§5 + [`archive/EXECVE_STACK_LEAK_OOM_HANG.md`](archive/EXECVE_STACK_LEAK_OOM_HANG.md) |
| Serial freezes under `-jN` concurrent build (first time at N>1; `-j1` clean), tail shows an `mmap`/`mprotect` burst (lazy-region count climbing) + 100% CPU + no `[OOM]` | Was a global-`LAZY_REGION_TABLE` alloc-under-lock site (rule-2, sibling of the above), **FIXED** by moving the map onto `Process::lazy_regions`: [`archive/LAZY_REGION_TABLE_ALLOC_UNDER_LOCK_HANG.md`](archive/LAZY_REGION_TABLE_ALLOC_UNDER_LOCK_HANG.md) §10. If this shape recurs, it is a *different* lock — grep the **full** log for `[OOM]` to separate an alloc-under-lock route from the §5.1 drain route |
| A `-jN` build gets *slower* per job as N rises (no deadlock, no `[OOM]`), `[FSCACHE]` slots pegged full with a climbing miss count, PMM free hovering near the reserve | File-backed mmap used to give **every process a private copy** of every page it touched, so N concurrent compilers held N copies of the same toolchain and re-read them N times — then evicted and re-read again. **FIXED** by `src/file_page_cache.rs`: [`reference/subsystems/memory.md`](reference/subsystems/memory.md) -> "Shared file pages" + [`runbooks/selfhost-kernel-build.md`](runbooks/selfhost-kernel-build.md) §5.1a. Check the `[FPCACHE]` PSTATS line; kill switch `SHARED_FILE_PAGES_ENABLED` |
| `[BKL] stuck: owner=0 waiter=N tag=511` bursts, later `[BKL] RECOVERED (advanced-lost)` — under fork/thread churn at SMP>1 | A **lost FIFO ticket**, not a stuck owner (`owner=0` = the lock is *free* while cores spin), and `tag=511` is only "profiler off". A barge (`acquire_no_ticket`) consumed a waiter's serving slot, the waiter re-ticketed, and the abandoned allocation froze the queue until a 20M-spin self-heal. **FIXED** — barges now leave the FIFO untouched: [`reference/subsystems/locking.md`](reference/subsystems/locking.md) + regression `sync::tests::kernel_lock_barge_against_waiters_does_not_leak_serving_slots` |
| `[SGI] POOL contended, skipped N ticks` climbing without bound; console still printing but ssh dead; onset during `[Cleanup] Thread N recycled` churn | **OPEN.** Not a crash — the box is *unscheduled*: `POOL` gates all preemption and a holder is waiting on something that needs another thread to run. The preemption watchdog cannot see it. The skipping tid is **not** the holder: [`reference/subsystems/scheduler.md`](reference/subsystems/scheduler.md) -> "The `POOL` gate" |
| `[STACK-OVERFLOW] tid=N ran off its 64KB kernel stack … kernel memory below it was corrupted` — **exactly `SMP-1` of them**, at boot, on an otherwise clean multi-core boot (tid 1..SMP-1) | Spurious. **FIXED 2026-08-16**: `adopt_current_as_core_idle` registered each secondary's boot stack as allocated but never painted its canary, and the sweep can't tell unpainted from smashed — so it also **masked** real overruns on those slots (the reporter latches on `base`). Now painted at registration. [`archive/SMP_SECONDARY_IDLE_STACK_CANARY.md`](archive/SMP_SECONDARY_IDLE_STACK_CANARY.md) + [`archive/DEVBOX_ISSUES.md`](archive/DEVBOX_ISSUES.md) -> "Issue 11". A *genuine* overrun looks the same but on a non-idle tid — see [`archive/EXTREME_SSHD_KERNEL_STACK_OVERFLOW.md`](archive/EXTREME_SSHD_KERNEL_STACK_OVERFLOW.md) |
| An SMP boot self-test asserts something about a secondary core's kernel stack and passes when it should not (e.g. `test_stack_canary_overrun_is_reported` reporting `spurious=0` on a build that visibly prints spurious overflows) | **FIXED 2026-08-17.** On the self-test image `bringup_secondaries()` runs *before* `threading::init`, and init walked every slot unconditionally — storing `FREE` over adopted cores' `RUNNING` state and re-pointing `stacks[i]`/`exception_stack_top` at fresh PMM stacks nothing was running on. Suite assertions then inspected pristine memory belonging to no one. Guarded by `is_adopted_core_idle`; regression `test_core_idle_slots_survive_init`. [`archive/SMP_ADOPTED_IDLE_SLOT_CLOBBER.md`](archive/SMP_ADOPTED_IDLE_SLOT_CLOBBER.md) |
| `SMP=4` boot suite stops emitting after ~23 tests (QEMU alive, serial log frozen) while `SMP=1` reports a clean 286/0 | A change to `blocking_relax` dropped its `yield_now`, reintroducing the spawn/reap cross-core wedge — the `SMP=1` suite cannot see it, its regression test SKIPs there. Fix: keep the yield in `blocking_relax`, use `blocking_relax_net` for the socket path only. [`archive/BLOCKING_RELAX_YIELD_SMP4_REGRESSION.md`](archive/BLOCKING_RELAX_YIELD_SMP4_REGRESSION.md), [`runbooks/debug-smp.md`](runbooks/debug-smp.md) |
| At `SMP>1` the secondaries report **online** (`✓ 3 secondary core(s) online`) and then run nothing at all — `smp_shared_{scheduler,userspace,migration}` FAIL with `core1=0`, all work stays on core 0, and the boot suite later hangs | The boot WFI probe disarmed their timers. IRQ 27 is a per-CPU PPI but `irq::register_handler` writes **one shared table**, so while the BSP probes, every secondary's tick lands in `probe_irq_nop` → `disarm()` → `msr cntv_ctl_el0, 0`; a secondary arms its tick exactly once at bringup and nothing re-arms it. **FIXED 2026-08-18** (`PROBING_CORE`: only the probing core disarms). Fastest check: the `core=[0-9]` histogram in the boot log — cores 1-3 absent means dead timers. [`archive/SMP_SECONDARY_TICK_KILLED_BY_WFI_PROBE.md`](archive/SMP_SECONDARY_TICK_KILLED_BY_WFI_PROBE.md) |
| Early boot is **silent** — no `Akuma Kernel starting...`, no `=== Memory Layout ===` — but the kernel clearly runs (later output appears, or it boots to ssh) | `safe_print!` writes to a hook registered by `OnceCopy`, and an unregistered hook **discards rather than panicking**. `src/main.rs` `rust_start` must install it as its FIRST statement; everything up to `akuma_exec::init` (~`:760`) prints before that call. Reference: [`reference/subsystems/primitives.md`](reference/subsystems/primitives.md) -> "Registration order matters". Background: [`archive/AKUMA_PRIMITIVES_EXTRACTION.md`](archive/AKUMA_PRIMITIVES_EXTRACTION.md) §7 |
| Rare SMP corruption or wedge with no obvious owner, **and** a boot log showing `[Test] preempt_guard_is_live` with `size=0` (or the test absent) | `PreemptGuard` lives in `akuma-primitives` behind `#[cfg(kernel_smp_shared)]`, emitted by *that crate's own* build.rs from *its own* forwarded feature. If the forwarding chain breaks the guard becomes a zero-sized no-op — nothing fails to build, nothing warns, and every inner-spinlock critical section silently loses preemption protection. `size=16` is healthy. Reference: [`reference/subsystems/primitives.md`](reference/subsystems/primitives.md) -> "Features are load-bearing". Background: [`archive/AKUMA_PRIMITIVES_EXTRACTION.md`](archive/AKUMA_PRIMITIVES_EXTRACTION.md) §6 |
| Console log lines look garbled / byte-interleaved between two unrelated messages under `smp-shared` | `with_irqs_disabled` only masks IRQs on the calling core, so two cores could both be inside `console::emit`'s UART write loop at once. **FIXED**, default-on in `release` (`kernel_console_lock` cfg, off only on the single-core `size`/`extreme-size` profiles): [`archive/UART_SMP_INTERLEAVE_FIX.md`](archive/UART_SMP_INTERLEAVE_FIX.md) + [`archive/DEVBOX_ISSUES.md`](archive/DEVBOX_ISSUES.md) Issue 3. Reference: [`reference/subsystems/console.md`](reference/subsystems/console.md) |
| You added a `log::debug!` / `log::info!` (or are reading one in the source) and **nothing ever prints**, on any profile, with every debug flag on | Working as built: **no logger is ever registered** — there is no `log::set_logger` call anywhere in `src/` or `crates/`, so all 68 `log::*` sites in the kernel compile to no-ops. They read as tracing and are not. Use `safe_print!` / `tprint!`, which is mandatory for console output anyway. Do not emit one from inside a spinlock hold — see [`reference/subsystems/console.md`](reference/subsystems/console.md) -> "Printing rules". Background: [`archive/TRIM_FAT_EMBARASSING_DUPLICATIONS.md`](archive/TRIM_FAT_EMBARASSING_DUPLICATIONS.md) §6 |
| Piping stdin over SSH hangs at **exactly 1 MiB** (256 KiB fine, stdout fine) — on the rump devbox but not on `disk.img` | sshd advertises a 1 MiB inbound channel window and never sends `SSH_MSG_CHANNEL_WINDOW_ADJUST`. Fixed in `userspace/sshd`, but **`devbox.img` carries an older `/bin/sshd`** — repopulate the image, don't read kernel source. [`archive/DEVBOX_ISSUES.md`](archive/DEVBOX_ISSUES.md) → Issue 12 + [`archive/SSHD_CHANNEL_WINDOW_NEVER_ADJUSTED.md`](archive/SSHD_CHANNEL_WINDOW_NEVER_ADJUSTED.md). General trap: the two images hold independent copies of every userspace binary and drift |
| You changed rump / sysproxy code, booted with `cargo run --release`, saw a clean boot — and verified nothing | `src/rump_proxy.rs` is *compiled* by the default feature set but only *runs* under `RUMP_NIC=1`. A default boot prints `[rump] BSP tap not available: tap: transport init failed` and skips it entirely. Use [`overlays/devbox/run.sh`](../overlays/devbox/run.sh) (ssh on `:2223`). [`archive/DEVBOX_ISSUES.md`](archive/DEVBOX_ISSUES.md) → Issue 12 "The general trap" |
| `bootstrap.sh` prints `Installing into disk:` with **nothing after the colon**, then a cheerful `OK: N MiB in 17 packages` — and the image ends up with no C toolchain, surfacing much later as a cargo **link** failure inside the guest | The package list never reached the container. `docker run -e VAR` (no `=value`) forwards `VAR` from the *caller's environment*, but a plain `VAR=...` in bash is a shell variable and is **not exported** — so docker passed nothing and `apk add` with zero arguments **exits 0**. **FIXED 2026-08-19**: pass the value explicitly (`-e VAR="$VAR"`), plus an empty-list guard and a post-install `[ -x /usr/bin/clang ]`-style check, because the failure is otherwise completely silent. General rule for this script: never trust `apk add`'s exit status to mean it installed what you meant. [`runbooks/build-devbox.md`](runbooks/build-devbox.md) |
| You set `PATH` for the devbox guest via `/etc/profile.d/*.sh` and it has no effect whatsoever | **`profile.d` is never read on this image.** busybox `ash` sources `/etc/profile` only for **login** shells; the devbox has no `/etc/profile` at all (`/etc` comes solely from `overlays/devbox/rootfs/`, which ships none); and every harness here drives the VM over `ssh host cmd`, which is non-login. Verified on a live guest 2026-08-19: `rust.sh` said `PATH=/usr/bin:$PATH` while the real PATH was the kernel default, and the dead file was removed. `PATH` comes from `akuma_exec::process::types::DEFAULT_ENV` ([`crates/akuma-exec/src/process/types.rs`](../crates/akuma-exec/src/process/types.rs)) — `/usr/local` ahead of `/usr` by design, which is what makes the `/usr/local` nightly toolchain beat an apk one. Change it there, not in a profile script. [`runbooks/build-devbox.md`](runbooks/build-devbox.md) → Toolchain note |
| In-guest `apk add` installs the files but exits non-zero — `failed to write database: Is a directory`, or `apk update` warns `updating and opening ...APKINDEX.tar.gz: Is a directory` | **FIXED 2026-08-19.** Not the post-install scripts: apk-tools 3's atomic writes probe `O_TMPFILE` (`openat(dirfd, ".", O_RDWR\|O_TMPFILE)`) whenever `/proc/self/fd` resolves, and `sys_openat` answered that with a **writable fd on the directory** — every later `write()` failed `EISDIR`, so the db/index writes died after a successful install. Now `O_TMPFILE` → `EINVAL` (apk falls back to `.tmp`+`renameat`) and write-opens of an existing directory → `EISDIR` at open() time. arm64 trap: `O_TMPFILE` is `0o20040000` (32-bit ARM fcntl values), *not* the asm-generic `0o20200000`. [`archive/APK_OTMPFILE_DIR_FD.md`](archive/APK_OTMPFILE_DIR_FD.md) |
| A syscall returns a different errno than Linux for the same call — most often success where Linux gives `EFAULT` | Known register, and **not** an audited one: [`archive/LINUX_COMPATIBILITY_ISSUES.md`](archive/LINUX_COMPATIBILITY_ISSUES.md). The recurring shape is a pointer check written as the *condition* of an `if`, so an unreadable pointer makes the arm skip and report 0 (`rt_sigaction`, `prctl`, `rt_sigtimedwait`). Absence of a syscall from that list means "nobody looked", never "matches Linux" |
| You are changing a syscall arm and want to know whether to call `validate_user_ptr`, `copy_to_user`, or the raw byte loop | [`reference/subsystems/syscalls.md`](reference/subsystems/syscalls.md) -> "Copying to and from user memory". Short version: `copy_to_user`/`copy_from_user`/`write_user_val`/`read_user_into` validate and prefault for you; pass `Prefault::No` inside a lock or an exception handler; keep a separate `validate_user_ptr` only when the check must precede an allocation, a lock, an fd allocation or a blocking wait. Background: [`archive/USER_COPY_FOLD.md`](archive/USER_COPY_FOLD.md) |
| A signal handler reads a garbage `siginfo_t`/`ucontext_t` field, or you are changing the signal-frame layout | The frame is a `#[repr(C)]` type — `akuma_exec::threading::sigframe::RtSigFrame` — with `offset_of!` assertions pinning every offset, so read the layout there rather than from arithmetic in `exceptions.rs`. Two known divergences from Linux are recorded and deliberate: the FPSIMD record sits at frame+584 (Linux: +592) and `sigcontext.__reserved` is 536 bytes (Linux: 4096). [`reference/subsystems/syscalls/signal.md`](reference/subsystems/syscalls/signal.md) -> "The `rt_sigframe` itself"; background: [`archive/REPR_C_SIGFRAME_STATX.md`](archive/REPR_C_SIGFRAME_STATX.md) |
| Kernel panic / OOM / allocation failure | [`runbooks/debug-memory-oom.md`](runbooks/debug-memory-oom.md) |
| PMM free pinned near the reserve, exec failures, "No current user process" cascades | Reclaimable memory is parked, not leaked: [`reference/subsystems/memory.md`](reference/subsystems/memory.md) -> "OOM decision map" + "Reclaim under memory pressure", and [`archive/OOM_KILL_DEFERRED_RECLAIM_GAP.md`](archive/OOM_KILL_DEFERRED_RECLAIM_GAP.md). **Corrected 2026-08-13:** this row used to say the tell is `[PSTATS]` showing `retired=N/Mp` — there is **no such line**. `retired_pages_pending()` / `retired_process_count()` have no non-test callers, so parked memory is currently invisible at runtime; surfacing it is a prerequisite for diagnosing this class |
| A process is SIGSEGV'd for OOM (or tcc/meow fails at the `extreme-size` 4 MB floor) while the box plainly has free memory moments later | Candidate: the user-page escalation's step 2 honors the 10 ms `PROCESS_RECLAIM_COOLDOWN_US` and **no step waits for it**, so memory parked more recently than that is not collected and the escalation gives up. [`reference/subsystems/memory.md`](reference/subsystems/memory.md) -> "KNOWN GAP: a premature give-up while memory is merely cooling". Unfalsifiable in the field until the parked-page counter is surfaced (row above), but the decision itself is now host-tested — `memmath::next_reclaim_step`, and `give_up_after_the_last_rung_is_the_known_premature_oom` pins this exact gap |
| An on-disk exercise (`cowstale`, `bssfork`, …) hangs or reports TIMEOUT, the boot log **stops growing**, and re-running passes | **Do not attribute this to your change without A/B-ing it several times.** Two separate flakes produce it, and both were mis-attributed to a code change during one session: **(1)** the intermittent SMP=1 exercise-suite wedge, ~40–50% per run, cause unknown, which hits between exercises where sshd forks the next one and can show **zero** time jumps ([`archive/COW_PILE_AUDIT.md`](archive/COW_PILE_AUDIT.md) §9 F8); **(2)** lost guest time, reported as `[WATCHDOG] Time jump detected` — `grep -ac 'Time jump detected' <log>`, where a high count means the run's timing is untrustworthy rather than naming a cause. `scripts/verify_trim.py` reports `smpN.host_timejumps` and, on a failed boot, `smpN.halt`. [`runbooks/verify-trim-fat-change.md`](runbooks/verify-trim-fat-change.md) -> "Known-benign" |
| You are reasoning about a CoW break and looking for the lock that serializes it across processes, or you found `cow_fault_lock(pa)` in an older doc | **There is no such lock, and there never was one.** `cow_fault_lock` was deleted 2026-08-13: it incremented a per-PA counter that nothing ever read or waited on, so two cores "acquired" it for the same PA simultaneously. What keeps the refcount sound is the **`released_last_va` gate** — each address space frees at most one global reference, and only for its own last VA on the frame. The comment at that gate in `src/exceptions.rs` is the explanation of record. Also: of the five CoW `ExecRuntime` fn pointers, only `cow_ref_inc` is ever called through the table (the audit says "3 of 4"; measured, it is 4 dead of 5). [`archive/COW_PILE_AUDIT.md`](archive/COW_PILE_AUDIT.md) §5 |
| PMM free count drifting down over a long run under a **threaded** workload (pthreads/Go), no `[OOM]`, no per-process leak — frames simply never come back | **One cause of this was fixed 2026-08-13 (F2).** `try_resolve_el1_cow_fault` resolved its owner with `read_current_pid()` instead of `address_space_owner_pid_for_fault()`, so for a `CLONE_VM` worker it tracked the new frame on a shared address-space view whose `Drop` discards its `user_frames` map, and skipped the `cow_ref_dec` for the old one — two frames per kernel-side CoW break, and threaded processes taking kernel-side user writes (futex wake, signal delivery) are the common case under load. The consequence was **never reproduced**, so if you are still seeing this drift, that means the hypothesis is unconfirmed, not that the fix regressed — look for another owner-resolution mismatch. Mechanism pinned by `test_cow_break_on_shared_view_leaks_both_frames`; [`archive/COW_PILE_AUDIT.md`](archive/COW_PILE_AUDIT.md) §4 (F2) |
| Kernel panic `Process table full (256 slots)` | **Known OPEN item**, not a capacity limit — the slots are reclaimable zombies and `register_process` panics instead of failing the spawn: [`reference/subsystems/memory.md`](reference/subsystems/memory.md) -> "OPEN: a full process table still panics the kernel" |
| Kernel panic with ESR_EL1/FAR_EL1 / "unhandled exception" / EL1 crash | [`runbooks/debug-exceptions.md`](runbooks/debug-exceptions.md) |
| Network doesn't work / can't connect out | [`runbooks/debug-network.md`](runbooks/debug-network.md) (smoltcp) / [`runbooks/debug-devbox.md`](runbooks/debug-devbox.md) (rump) |
| `cargo` or `rustc` crashes in the devbox | [`runbooks/debug-devbox.md`](runbooks/debug-devbox.md) -> Toolchain crashes |
| `git clone` hangs or wedges | [`runbooks/debug-devbox.md`](runbooks/debug-devbox.md) / [`runbooks/debug-network.md`](runbooks/debug-network.md). Original pipe-deadlock theory never reproduced on a 2026-08-11 revisit — **OPEN**: [`archive/DEVBOX_ISSUES.md`](archive/DEVBOX_ISSUES.md) -> "Issue 1" |
| `git clone` dies instantly with `Alarm clock`, or dies intermittently mid-checkout with exit code 130 | Two `ITIMER_REAL` bugs, both **FIXED** 2026-08-11: a stale itimer deadline surviving thread-slot reuse, and `check_itimers` force-interrupting an `SA_RESTART` handler's blocking syscall. [`archive/GIT_CLONE_STALE_ITIMER_SIGALRM.md`](archive/GIT_CLONE_STALE_ITIMER_SIGALRM.md) |
| SSH echo lag / staggering / terminal sizing | [`runbooks/debug-ssh-latency.md`](runbooks/debug-ssh-latency.md) |
| Piping a large file over SSH (`ssh host 'cat > f' < big`) stops dead at **exactly 1048576 bytes**, then the client times out with `Timeout, server localhost not responding` | sshd advertised a 1 MiB channel window and never sent `SSH_MSG_CHANNEL_WINDOW_ADJUST`, so >1 MiB of stdin could never arrive. **FIXED** 2026-08-13, together with the kernel's drop-oldest stdin buffer that the same 1 MiB coincidence had been hiding: [`archive/SSHD_CHANNEL_WINDOW_NEVER_ADJUSTED.md`](archive/SSHD_CHANNEL_WINDOW_NEVER_ADJUSTED.md). Don't read `wc -c` alone here — two subsystems picked the same constant |
| An otherwise-idle interactive SSH session (nothing mid-transfer) eventually drops with `Timeout, server localhost not responding` | Different bug, same client-side message as the row above. sshd never replied to `SSH_MSG_GLOBAL_REQUEST` (e.g. OpenSSH's periodic `keepalive@openssh.com`, sent when the client sets `ServerAliveInterval`) — an unanswered `want_reply` request silently fell into a catch-all, so after `ServerAliveCountMax` misses the client gave up. **FIXED** 2026-08-24: [`../userspace/sshd/docs/SSH_KEEPALIVE_TIMEOUT_FIX.md`](../userspace/sshd/docs/SSH_KEEPALIVE_TIMEOUT_FIX.md) |
| Ctrl-C does nothing to a foreground command that doesn't itself read stdin (e.g. `tail -f file` under `/bin/sh` over `ssh -tt`) — it just keeps running | Akuma had no tty line discipline: `TerminalState`'s `ISIG`/`VINTR` fields were tracked but never consulted, so the INTR byte was just forwarded (or, in canonical mode, buffered) as ordinary data. **FIXED** 2026-08-24 — real terminal-signal semantics live in the kernel now, at the single chokepoint every stdin write already goes through: [`archive/CTRL_C_SIGINT_DELIVERY.md`](archive/CTRL_C_SIGINT_DELIVERY.md) |
| **Every** ssh session dies instantly on the **extreme-size** profile — SIGSEGV inside `/bin/sshd` at a faulting address that makes no sense (`FAR=0x10`), interactive PTY included | Not an sshd bug and not the ELF loader: the profile's 64 KB user-thread **kernel** stack was ~10 KB too small for the session path, and the overrun zeroed PTEs in the session process's own page table. **FIXED** 2026-08-13, along with the stack canary that was painted but never checked: [`archive/EXTREME_SSHD_KERNEL_STACK_OVERFLOW.md`](archive/EXTREME_SSHD_KERNEL_STACK_OVERFLOW.md). A `[STACK-OVERFLOW] tid=N ran off its NKB kernel stack` line now names this class directly |
| Every ssh session dies at `kex_exchange_identification: Connection reset by peer` on the **rump devbox** (`:2223`), with no rump DHCP lease on the console | The missing lease is a red herring — `rump_server` logs to `/var/log/box/0/rump_server.log` in the image, not the console, and DHCP was always working. Real cause: `RumpSocket` was the one fd family `clone_deep_for_fork` did not refcount, so sshd's post-`fork` `drop(stream)` closed its own session child's socket. **FIXED** 2026-08-13: [`archive/RUMP_SSHD_FORKED_SESSION_CLOSES_SOCKET.md`](archive/RUMP_SSHD_FORKED_SESSION_CLOSES_SOCKET.md) |
| `tcc` dies with `tcc: error: file 'tcc' not found` (or any non-busybox program sees a stray first argument) when run from a `paws` shell — e.g. on **extreme-size**, whose login shell is paws | `paws` passed `args[0]` (the command name) through to `spawn`, which prepends `argv[0]` itself, so the name arrived twice. busybox hides it (a leading applet name is how a multicall binary is invoked); tcc read it as an input file. **FIXED** 2026-08-13: [`archive/PAWS_DUPLICATED_ARGV0.md`](archive/PAWS_DUPLICATED_ARGV0.md). Not a profile bug — reproduces on `release` via `paws -c` |
| A network client works against a fast endpoint and hangs against a slow one — a request whose first response byte is delayed several seconds never returns, while the identical request answered in ~1 s streams perfectly. Or a tokio/hyper client hangs intermittently having sent **nothing** on an ESTABLISHED socket | **Four defects, all FIXED 2026-08-17** — and neither the "delayed first byte" nor the "lost wakeup" framing was right. The dominant one: a socket in `SynSent` answers `is_active() && !may_recv()`, the same pair a FIN produces, so a *connecting* socket was reported `EPOLLIN`+`EPOLLRDHUP` with `recv()==Ok(0)` and the client parked forever before sending its request. Plus a 30 s blocking-read cap (which also killed **mid-stream** reads), an unimplemented `SO_RCVTIMEO`, and an `EPOLLET` write edge that was never re-armed. [`archive/SOCKET_DELAYED_FIRST_BYTE_HANG.md`](archive/SOCKET_DELAYED_FIRST_BYTE_HANG.md) § Resolution; procedure + probes in [`runbooks/debug-delayed-first-byte.md`](runbooks/debug-delayed-first-byte.md). Races both — repeat `nettest-reqwest post <url> 64` a dozen times, a single run proves nothing |
| The terminal **stutters** — a TUI (nca) redraws in visible steps, output arrives in bursts, and it gets worse while the network is busy | **FIXED 2026-08-18.** Not networking and not a lock: `nanosleep` had a hard floor of **~35.5 ms regardless of the requested duration**, because an expired sleeper joined the back of the round-robin queue (one timeslice per runnable thread) — eligibility was not execution. sshd's bridge asks for `sleep_ms(1)`, so all terminal output was forwarded at ~27 Hz. Fix: the deadline wake-pass now arms a run-next hint (`WAKE_DEADLINE_PREEMPT`) **and** the tick is 1 ms (10 ms kept on `extreme-size`). Result: sleep/poll floors ~1 ms, 0 stalls in 1500 writes, pipe round-trip 3× faster, 128 MB download 2× faster, boot suite green on release SMP=1/4 and devbox. Full A/B + open follow-ups (rump, tick sweep, full kernel-build): [`archive/SCHEDULING_INVESTIGATION.md`](archive/SCHEDULING_INVESTIGATION.md) → Resolution |
| A program that spawns children through an async runtime (tokio `Command::output()`, `nca`'s `execute_bash`, Go's `exec.Cmd`) has **every** call time out, while the kernel log shows the child running and exiting in milliseconds and a successful `wait4` — and the same command from an SSH shell returns instantly | **Two defects, both FIXED 2026-08-17**, and neither is a lost wakeup. `read()` on an `O_NONBLOCK` pipe ignored the flag and **blocked** (only the `PipeRead` arm; its `ChildStdout`/`UnixSocket` siblings were fine), stalling the reactor thread inside the kernel. And a pipe's `EPOLLET` `EPOLLIN` edge could never be re-armed, so the *drained → EOF* transition produced no new bit and was suppressed forever — `read_to_end` waited on a pipe already at EOF. [`archive/TOKIO_PIPE_EPOLL_HANG.md`](archive/TOKIO_PIPE_EPOLL_HANG.md); procedure + probes in [`runbooks/debug-async-subprocess-hang.md`](runbooks/debug-async-subprocess-hang.md). Same class as the socket `EPOLLOUT` edge above — suspect `epoll_check_fd_readiness` first, and turn on `SYSCALL_DEBUG_EPOLL_EDGE` |
| A client's `SO_RCVTIMEO` / `SO_SNDTIMEO` never fires, or a read dies with `TimedOut` at a deadline nothing configured | Both options were accepted by `setsockopt` and silently dropped (no `getsockopt` arm at all), and blocking TCP reads were capped at 30 s / writes at 5 s by the kernel itself. **FIXED 2026-08-17.** If you see it again, [`reference/subsystems/networking.md`](reference/subsystems/networking.md) -> "Divergences from Linux"; measure with `nettest-std rcvtimeo` |
| Every SSH connection dies at `kex_exchange_identification: read: Connection reset by peer`, but sshd is alive and looping `accept` — and another listener (`httpd` on :8080) still serves fine | Someone destroyed the listener's socket **slot**, not the network. Check sshd's `[PSTATS]` line: `accept=N` climbing with **no** `clone`/`write`/`close` means zero connections were ever accepted. Usual cause is a double `remove_socket()` on a recycled slot — release socket fds through `sys_close`, never a bare `remove_socket`. [`reference/subsystems/networking.md`](reference/subsystems/networking.md) -> "Socket lifetime". The identical line is also just an exhausted listener backlog (`socket::MAX_BACKLOG`, 8 by default), so rule that out first |
| A `127.0.0.1` round trip is slower than a round trip that goes out through the NIC and back (`redis-benchmark`/`httpd` against localhost vs. through the host port forward) | **Two fixes, 2026-08-20.** `LoopbackAwareDevice`'s internal queue was a `VecDeque<Vec<u8>>` (per-packet heap alloc, unbounded growth) — now a fixed-capacity ring. That alone didn't touch latency: a loopback push never rang the cross-core doorbell the NIC's virtio interrupt rings, so a loopback-blocked waiter rode the old tick-bound cadence `AKUMA_NET_ISSUES.md` fixed for external traffic (measured ~7x slower at p50 than the NIC path). Wiring a loopback push into that same doorbell (`akuma_net::runtime::NetRuntime::wake_netpoll` -> `ring_netpoll_doorbell`) cut p50 4.5x (5.4 ms -> 1.2 ms), now ~1.6x the NIC path. p90/p99 still trail it by a smaller, unchased margin. [`archive/LOOPBACK_RING_CONVERSION.md`](archive/LOOPBACK_RING_CONVERSION.md) |
| A network server's throughput is **flat** as clients are added — 4 clients and 32 clients get the same total ops/s, and per-client latency rises exactly linearly | Not slowness, serialization. Throughput is `1 / (us spent in smoltcp_net::poll() per round trip)`, because `poll()` runs under the one global `NETWORK` spinlock inside an IRQ-masking `PreemptGuard`. Predicts the measured plateau within 2 % at SMP=1/2/4 (165/83/69 us -> 6.1k/12.1k/14.4k rps). More cores cannot help past 2 (only two actors: the drain loop and single-threaded server). Measure with `scripts/benchmarks/redis_conc_sweep.py` — a **single** client count or a serial harness cannot see this at all. [`archive/REDIS_ROUND_TRIP_CEILING.md`](archive/REDIS_ROUND_TRIP_CEILING.md) |
| `[NICSTAT]` shows ~2 tx packets per rx packet on a request/response workload | Delayed ACK was disabled (`set_ack_delay(None)`), so smoltcp ACKed each request *before* the server replied and the reply could not piggyback — a second `add_notify_wait_pop` spin per round trip inside the `NETWORK` lock. **FIXED 2026-08-24** (`Some(10ms)`): tx/rx 1.96 -> 0.98, ceiling 14,085 -> 20,202 rps, c=1 latency −48 %. Costs −19 % on 64 KB receive-heavy bulk; shortening the timer does **not** buy that back. [`archive/REDIS_ROUND_TRIP_CEILING.md`](archive/REDIS_ROUND_TRIP_CEILING.md) §3 |
| `redis-benchmark` pegs one host core at 100 % while QEMU sits at single digits and the run never finishes | **Not a guest hang** — the guest stays healthy (`redis-cli ping` answers instantly, `[NICSTAT]` windows stay contiguous, client sockets show `Recv-Q=7`, the unread reply). It is a `while(1)` in `redis-benchmark`'s own `writeHandler` that spins with **zero syscalls** when one write returns `EAGAIN`. Kill and retry; nothing in the guest needs recovering. [`archive/REDIS_BENCHMARK_HOST_CONTENTION_LIVELOCK.md`](archive/REDIS_BENCHMARK_HOST_CONTENTION_LIVELOCK.md) |
| A benchmark number is ~2x worse than the last time you measured the same code | Check VM uptime before suspecting code. A long-running devbox that has already served a workload plateaued at 7,634 rps where the **same commit** freshly booted did 14,085 — and that gap was nearly filed as a code regression. Boot fresh per arm (`scripts/benchmarks/redis_smp_sweep.py` does). [`archive/REDIS_ROUND_TRIP_CEILING.md`](archive/REDIS_ROUND_TRIP_CEILING.md) §5 |
| Guest network throughput will not go above ~20,000 round trips/s however much you optimise the kernel | ~19 us of the ~44 us per-round-trip budget is **one MMIO `QueueNotify` write per packet** — a vmexit in which QEMU runs the virtio-net TX path (with SLIRP, the NAT and host-socket write) *inline*. Proven fixed-cost: byte-independent (16.4 us/pkt at 61 B and at 1288 B), does not amortise over a 92,803-packet burst, and **identical whether transmit blocks or not** (19.5 us blocking vs 19.9 us with `net-noalloc`'s non-blocking submit). Not addressable from inside the kernel — it needs a different netdev backend. Do **not** try suppressing the notify: measured 2026-08-19 at p99 6,747 us, see `smoltcp_net::nic_kick_tx`. [`archive/REDIS_ROUND_TRIP_CEILING.md`](archive/REDIS_ROUND_TRIP_CEILING.md) §5d |
| A single client gets ~750 us per round trip while the server plateaus at 14,000 rps — and adding cores makes c=1 *slower* | Two different limits. The plateau is the `NETWORK` serialization above; the c=1 number is **not in the poll budget at all**. 757 us/rt at c=1, of which `poll()` is 69 — **688 us (91 %) is wake + schedule latency `[NICSTAT]` cannot see**. Tells: SMP=4 is slower than SMP=2 at c=1 (1,321 vs 1,380), c=2 -> c=4 jumps 4.8x, and a VM with a spinning core is 4.6x *faster* here while half as fast at c=32. [`archive/REDIS_ROUND_TRIP_STAGE_TRACE.md`](archive/REDIS_ROUND_TRIP_STAGE_TRACE.md) §4 |
| Considering a per-socket netlock, or finer `NETWORK` granularity, to fix network throughput | Measure `poll_wait` before writing lock code — it is already recorded. `NETWORK` is **97.3 % utilized and 3.2 % contended**: saturated, not queued, because there are only two actors handing off. Finer locks redistribute a 2.2 us handoff and leave 66 us of serialized work untouched. `crates/akuma-net/src/locks.rs` already contains this design, is dead code, and cannot work as written (the guard drops before the function returns). [`archive/REDIS_ROUND_TRIP_STAGE_TRACE.md`](archive/REDIS_ROUND_TRIP_STAGE_TRACE.md) §2 |
| Per-`poll()` cost is much worse at SMP=1 than SMP=4 and you suspect lock contention or the socket-table walk | Neither — contention *grows* with cores (0.0 -> 2.2 us) and the walk is *larger* at SMP=4 (83 sockets vs 66). The residual (smoltcp state machine) is **31.0 / 13.7 / 9.6 us per poll call at SMP=1/2/4** and accounts for **89 % of the whole SMP gain**. One core makes the netpoll loop and the server timeshare, and each switch costs the poll path its cache/TLB working set. No locking change addresses it. [`archive/REDIS_ROUND_TRIP_STAGE_TRACE.md`](archive/REDIS_ROUND_TRIP_STAGE_TRACE.md) §3 |
| A `[NICSTAT]` `max=` field shows a single operation taking milliseconds | Host deschedule, not guest behaviour — the guest clock is a physical counter that runs while the vCPU thread is off-core, so a mid-vmexit preemption reads as a multi-ms instruction. Seen at 439x the mean (6,591 us `tx_wait` against 15.0 us). Window `dt=` stays 5,000-5,002 ms throughout, so **averages are safe and every p99/max is measuring the host's scheduler**. [`archive/REDIS_ROUND_TRIP_STAGE_TRACE.md`](archive/REDIS_ROUND_TRIP_STAGE_TRACE.md) §6b, §7 |
| epoll crash / DNS hang under bun | [`runbooks/debug-network.md`](runbooks/debug-network.md) -> epoll |
| HTTPS download measured in minutes | [`runbooks/debug-network.md`](runbooks/debug-network.md) -> TLS |
| Fork / exec / signal misbehaviour | [`reference/subsystems/syscalls/proc.md`](reference/subsystems/syscalls/proc.md) + [`syscalls/signal.md`](reference/subsystems/syscalls/signal.md) + [`scheduler.md`](reference/subsystems/scheduler.md) |
| A threaded program dies by **SIGSEGV with `FAR=0`** where you expected SIGABRT (cargo reporting `signal: 11` and `signal: 6` for the same crate on different runs) / `abort()` returns / `kill -TERM` does nothing | Not memory corruption — an undelivered signal, and musl's `a_crash()` is the `FAR=0` store. Two causes, both **FIXED**: `tkill` was handed a PID instead of a thread slot, and fatal `SIG_DFL` signals pended while blocked were dropped at syscall return. [`archive/SELFHOST_DEVBOX_SMOLTCP.md`](archive/SELFHOST_DEVBOX_SMOLTCP.md) -> "SIGABRT delivery" + [`syscalls/signal.md`](reference/subsystems/syscalls/signal.md) -> "Default action for pended signals". Regress with `userspace/forktest/c_stress/abortsig.c` |
| Filesystem / ext2 / procfs errors | [`reference/subsystems/vfs.md`](reference/subsystems/vfs.md) + `archive/EXT2_FIRST_DATA_BLOCK_FIX.md` |
| A file written by multiple threads of one process (shared fd, `CLONE_FILES`) comes out with corrupted/torn content but the right total size — no crash, no error | Was a TOCTOU race on the file position: `sys_write` read `.position` from a cloned fd entry, wrote to disk, then wrote the position back — two threads racing `write()` on the same fd could read the same stale position and overwrite each other's bytes. **FIXED** (2026-08-07): [`archive/CONCURRENT_WRITE_POSITION_RACE.md`](archive/CONCURRENT_WRITE_POSITION_RACE.md). `O_APPEND` has an unfixed sibling race (still derives its offset from `file_size()` per write) |
| Porting a new binary (missing syscalls) | [`reference/subsystems/syscalls.md`](reference/subsystems/syscalls.md) -> Porting + `archive/*_MISSING_SYSCALLS.md` |
| `mount`/`umount2` return `EPERM` inside a box (deliberate — see [`reference/subsystems/containers.md`](reference/subsystems/containers.md) -> "Mount policy"), or want a second disk mounted, or `MS_REMOUNT` | **`umount2`, `MS_REMOUNT`, and multi-disk `mount(2)` (fstype `"ext2"`) all FIXED 2026-08-24** — [`reference/subsystems/syscalls/container.md`](reference/subsystems/syscalls/container.md) § "mount / umount2". Audit of record (now historical): [`archive/MOUNT_MISSING_SYSCALLS.md`](archive/MOUNT_MISSING_SYSCALLS.md) |
| `free`/`uptime`/`df`/native-`ifconfig` broken, or want a read-only rootfs and the minimal busybox applet set for dev | Clustered gap inventory (procfs `/proc/{meminfo,cpuinfo,uptime,mounts,net/dev}`, absent sysfs, `MS_RDONLY` dropped, `statfs` nr 43, `SIOCGIF*` ioctls): [`archive/MINIMAL_DEV_BUSYBOX_APPLETS.md`](archive/MINIMAL_DEV_BUSYBOX_APPLETS.md). Tier 1 + Tier 2 verified 2026-08-12; three new bugs found (`utimensat` ⇒ V1, `getgroups` ⇒ V2, missing `/etc/passwd` ⇒ V3). Shell applets (`head`/`tail`/`sed`/...) pass. **`ifconfig` (`/proc/net/dev` + `SIOCGIF*`) FIXED 2026-08-24** — [`reference/subsystems/networking.md`](reference/subsystems/networking.md) § "Interface introspection". **`/proc/mounts`, `statfs` nr 43, and `MS_RDONLY` reporting FIXED 2026-08-24** — [`reference/subsystems/vfs.md`](reference/subsystems/vfs.md) § "Mount table". `meminfo`/`cpuinfo`/`uptime`/sysfs gaps are still open |
| `std::thread::sleep()` panics — `` assertion `left == right` failed: left: 38, right: 4 `` at `library/std/src/sys/thread/unix.rs` | Was a missing syscall: Akuma never implemented `clock_nanosleep` (#115), which is what `thread::sleep` actually calls on `linux` targets (not plain `nanosleep`) — every unimplemented syscall returns `ENOSYS` (38), which fails std's internal `assert_eq!(r, EINTR)` (4). **FIXED** (2026-08-07): [`archive/THREAD_SLEEP_MISSING_CLOCK_NANOSLEEP.md`](archive/THREAD_SLEEP_MISSING_CLOCK_NANOSLEEP.md). If you're chasing an SMP hang and a watchdog/health-check thread went silent on an *old* disk image/kernel, suspect this first |
| A/B run shows no difference / boot doesn't match the features you built | [`reference/subsystems/locking.md`](reference/subsystems/locking.md) -> playbook rule 5 + `archive/BKL_VFS_CARVE_OUT.md` §17 |
| `[BKL] RECOVERED` lines under SMP load / "can I just delete the BKL?" | [`reference/subsystems/locking.md`](reference/subsystems/locking.md) -> "What the BKL is still the only lock for" + [`archive/BKL_PHASE7_AUDIT.md`](archive/BKL_PHASE7_AUDIT.md) |
| `[BKL] stale dropped-window ... healed` / converting a syscall off the BKL | [`reference/subsystems/locking.md`](reference/subsystems/locking.md) -> "The per-syscall BKL opt-out list" + [`archive/BKL_PHASE7F_OPTOUT_LIST.md`](archive/BKL_PHASE7F_OPTOUT_LIST.md) |
| `test_epoll_multi_poller_pipe FAILED: woken=1` / a boot-suite test that fails ~1 boot in 3 | [`archive/EPOLL_MULTI_POLLER_PIPE_FLAKE.md`](archive/EPOLL_MULTI_POLLER_PIPE_FLAKE.md) — test defect, not a kernel bug; do not accept/reject a change on one boot |
| Benchmarking in-VM rustc / SMP scaling vs Linux | [`archive/BKL_RUSTC_SCALING_BASELINE.md`](archive/BKL_RUSTC_SCALING_BASELINE.md) + `scripts/bkl_rustc_bench/` |
| `rustc -O big.rs` produces no artifact and prints nothing / `(opt cgu.N) SIGSEGV` | **Both modes root-caused + FIXED.** SIGSEGV = cross-core stack-sharing in the scheduler: [`archive/SMP_SHARED_ONCPU_GATE.md`](archive/SMP_SHARED_ONCPU_GATE.md). The residual *hang* = a stale thread-slot index letting one rustc's teardown kill another rustc's linker: [`archive/STALE_THREAD_SLOT_KILL.md`](archive/STALE_THREAD_SLOT_KILL.md) |
| A process sits in `ps`/`PSTATS` with a frozen syscall count forever / its parent's `wait4` never returns | A process left with **no thread**. `[PROC-ORPHAN]` (next to `[THR-DUMP]`) names it directly; `[TERM]` names the site that killed its thread. Origins: [`archive/STALE_THREAD_SLOT_KILL.md`](archive/STALE_THREAD_SLOT_KILL.md) (`unregister_process`, FIXED) and [`archive/GRACE_EXPIRED_HARD_KILL_ORPHANS.md`](archive/GRACE_EXPIRED_HARD_KILL_ORPHANS.md) (`kill_thread_group` grace expiry, FIXED). Check for `stale tid=` / `[KTG-STALE]` guard fires |
| A `-j4` build stalls with waiters parked in untimed `FUTEX_WAIT` for hundreds of seconds, ~3 % CPU, but **no** `[FUTEX-ORPHAN]` and correct key namespaces | Usually **not** a futex bug — the stuck waiter is a parent waiting on a process that has no thread to run. Check `[PROC-ORPHAN]` first: [`archive/GRACE_EXPIRED_HARD_KILL_ORPHANS.md`](archive/GRACE_EXPIRED_HARD_KILL_ORPHANS.md). Note `[kill]` lines prove nothing under `smp-shared` (deferred kills self-mark) — read `[TERM]` instead |
| cargo `-j4` waits forever after `build failed, waiting for other jobs…`; a rustc child idles in `read()` on a pipe whose `[PIPE-DUMP]` shows `writers>0` but **no** `/proc/*/fd` holds the write end | A leaked pipe write refcount from a forged exit: `kill_thread_group` PHASE 2 stamped `set_exited()` through a recycled tid, the parent reaped the live linker mid-run, and its abandoned fd teardown leaked the refs — **FIXED** (2026-08-07): [`archive/KTG_STALE_TID_EXIT_STAMP_J4_HANG.md`](archive/KTG_STALE_TID_EXIT_STAMP_J4_HANG.md). Guard fires print `[KTG-STALE-CH]` |
| A sibling thread in a killed group survives forever, parked in an *untimed* `FUTEX_WAIT` — no `[KTG-STALE]` line names it, `[PROC-ORPHAN]` is 0, and the group's `wait4` never returns | A thread that never reaches the EL1→EL0 boundary never consumes its pending-kill flag, so `kill_thread_group`'s grace-wait mistook "flag no longer pending" for "thread died" and its hard-kill gate refused on the same (correct) ownership evidence — sparing it twice. **FIXED** (2026-08-07), Failure C in [`archive/J4_WRITE_PERM_FAULT_AND_HALF_WRITTEN_LINKER_OUTPUT.md`](archive/J4_WRITE_PERM_FAULT_AND_HALF_WRITTEN_LINKER_OUTPUT.md) §4a: `grace_kill_should_terminate` now gates on ownership alone |
| SMP=4 `[BKL] stuck owner=N tag=511` storm (boot or load), EL1 crash with `ELR=0x8` / SP outside thread stack | **Partially fixed** — the ON_CPU scheduler gate killed the EL1-crash class: [`archive/SMP_SHARED_ONCPU_GATE.md`](archive/SMP_SHARED_ONCPU_GATE.md) + [`runbooks/debug-smp.md`](runbooks/debug-smp.md) first row. A storm **without** crashes still reproduces on gated kernels (2026-08-06: boot-suite "NEON across preemptive scheduling" onset, owner core holding near-permanently; also 81 events in a `-j4` run). Measure as a rate when A/B-ing, not a boolean — some boots recover, some wedge SSH |
| A thread parked forever in an unreturned `futex` (`sc=-1 tsc=98` in `[THR-DUMP]`), low CPU, frozen syscall count | [`runbooks/debug-futex-lost-wakeup.md`](runbooks/debug-futex-lost-wakeup.md) — it separates the four causes that share this one symptom, starting from whether `[FUTEX-DUMP]` still has the waiter queued. Five earlier divergences (chiefly **requeued waiters never removed from the requeue target**) are **FIXED**: [`archive/FUTEX_REQUEUE_LOST_WAKEUP.md`](archive/FUTEX_REQUEUE_LOST_WAKEUP.md). Current behaviour: [`reference/subsystems/syscalls/sync.md`](reference/subsystems/syscalls/sync.md) |
| Several copies of one musl binary run at once and one wedges in `futex` at an address they all share (e.g. `0x300c2340`) | Non-private futexes were keyed by **virtual address alone**, so every process's musl `__thread_list_lock` shared one queue and wakes were stolen across processes — **FIXED** (2026-08-04): [`runbooks/debug-futex-lost-wakeup.md`](runbooks/debug-futex-lost-wakeup.md) §5. Regress with `userspace/forktest/c_stress/futexkey.c` (deterministic); the 8×`futextest_rs` stress run passes on **both** arms and cannot detect it |
| `mprotect` appears to do nothing: a `PROT_NONE` guard page stays writable, RELRO stays writable, a stack overflow scribbles instead of faulting | **FIXED** (2026-08-05) — `flush_tlb_range` invalidated with `tlbi vale1is`, whose ASID field is zero for every user VA while user processes run under non-zero ASIDs, so it matched nothing. Now `vaae1is` (all-ASID), which is required because `new_shared` aliases one L0 table under several ASIDs: [`runbooks/debug-thread-spawn-segv.md`](runbooks/debug-thread-spawn-segv.md) §2b. Regress with `userspace/forktest/c_stress/mprotectlb.c` |
| A `pthread_create`d thread dies `SIGSEGV after 0.00s` at a **fixed** `ELR` with near-null `FAR` (often exactly `0x8`), preceded by `sig 11 needs sigaltstack but slot N has none — re-pending`, and the group is killed by `SIGSEGV in clone_thread` | **FIXED 2026-08-06** — `clone_thread` wrote the child TID into the `CLONE_CHILD_CLEARTID` pointer *at clone time*; Linux only writes that word (with zero) at child **exit**. musl passes `&__thread_list_lock` there, so every spawn stamped a live tid into musl's thread-list mutex and `__tl_lock`'s `val == tid` fast path handed the lock to the new child, which then unlinked itself with a NULL `self->prev` (`str x0,[x1,#8]` → write to `0x8`). [`runbooks/debug-thread-spawn-segv.md`](runbooks/debug-thread-spawn-segv.md) §2e. Regress with `userspace/forktest/c_stress/tidflags.c` (4 FAIL before, 8 PASS after, 8 PASS on Linux) |
| An in-VM `-j4` build takes an hour instead of minutes; a VM under load stops answering ssh | The UART, not the kernel: `[IA-DP]`/`[pipe]`/`[mmap]` printed per event and saturated the shared console (~270 KB/s, four cores contending for its lock). Gated 2026-08-08 — the same build went green in 2m21s. [`archive/SERIAL_TRACE_TRAFFIC_AUDIT.md`](archive/SERIAL_TRACE_TRAFFIC_AUDIT.md). Also: a wedged VM prints **nothing**, so check console-log growth for liveness, never ssh reachability |
| A multi-threaded process `fork`s and dies `EXIT=139` writing one of its **own globals**, `[WPF] cow_ref=0 lazy_self=NONE eager=NONE ap_rw=true` | **FIXED 2026-08-08** — the write was legal: a sibling thread had already broken CoW on that page, and the fault was judged against the consumed state. An ELF `.data`/`.bss` page has no `mmap` region, so no repair path would claim it. `stale_write_fault_absorbed` re-reads the PTE first: [`archive/CARGO_NULL_RC_MEMORY_REFERENCE_AUDIT.md`](archive/CARGO_NULL_RC_MEMORY_REFERENCE_AUDIT.md) §12. Regress with `userspace/forktest/c_stress/bssfork.c` (8/8 SEGV before, 0 after, PASS on Linux) |
| An in-VM `-j4` build wedges the box **silently**: no console at all (not even `PSTATS`), ~400% host CPU, ssh dead, BKL **idle** | **FIXED 2026-08-08** — a `TALC`↔`PMM` lock cycle, not the BKL: `alloc_pages` built its `Vec` while holding `PMM`, and heap growth takes `PMM`. Attach with `GDB=1` + [`../scripts/lockprobe.py`](../scripts/lockprobe.py); the tell is `TALC` and `PMM` lock bytes both `0x01` with every core in a spin loop. [`archive/PMM_TALC_LOCK_CYCLE_SILENT_WEDGE.md`](archive/PMM_TALC_LOCK_CYCLE_SILENT_WEDGE.md). It is silent because the `PMM` side spins inside `with_irqs_disabled`. Do **not** confuse with the `[BKL] stuck` storm — that prints thousands of lines and has `owner != 0` |
| A `[Fault]` address looks like a pointer with one bit missing | Check the ELF before believing it: `readelf -sW <bin>` on `FAR`. The `cowstale` fault read as "a pointer that lost bit 28" for a day; the address was a `.bss` global and the base register held `&`the-section, not data. §12 of the audit above |
| In-VM `cargo build` finishes (`Finished … profile`) then dies with `EXIT=139`, `[WILD-DA] pid=<cargo> FAR=0x0` (or near-null) at a `Drop::drop` PC, ELF on disk valid | **FIXED 2026-08-14** — `MADV_DONTNEED` on a CoW-shared frame `memset` the **peer's** live page (0 of 4096 bytes surviving); cargo forks per rustc invocation, so its heap is exactly that shape. The corruption is proven deterministically; that Defect B *took* this route is a strong inference, not a measurement — confirm it by reading `dontneed_shared_frame` from the `[MADV]` PSTATS line during a build, and if it reads 0 the hunt is not over. [`archive/MADV_DONTNEED_SHARED_FRAME.md`](archive/MADV_DONTNEED_SHARED_FRAME.md); current rules in [`reference/subsystems/syscalls/mem.md`](reference/subsystems/syscalls/mem.md). Regress with `userspace/forktest/c_stress/madvshared.c` (2 FAIL before, ALL PASS after, ALL PASS on Linux arm64). To symbolize a *new* one: ELR against cargo's text (base `0x10000000`, `filesz=0x1da1c6c`) — `0x4e48c8`/`0x7d7bc4` are `Rc<UnitInner>` drop glue, `0x14d1ad4` is `<semver::Identifier as Drop>::drop`. Earlier audit that narrowed it (and ruled out PMM-level UAF): [`archive/CARGO_NULL_RC_MEMORY_REFERENCE_AUDIT.md`](archive/CARGO_NULL_RC_MEMORY_REFERENCE_AUDIT.md) §13 |
| A thread faults and the dump says `*** AS MISMATCH: L0 BASE DIFFERS ***` | **FIXED 2026-08-06 — the same bug as the `N × 0x30000000 + 0x6c964` row above.** `ttbr0_live != ttbr0_proc` is the signature of `entry_point_trampoline` having resolved the wrong `Process` and `run()` having activated *its* address space. Fixed by resolving via `THREAD_PID_MAP`: [`runbooks/debug-thread-spawn-segv.md`](runbooks/debug-thread-spawn-segv.md) §2h. The earlier §2g `THREAD_STATES` candidate fix was landed 'pending -j4 proof' and was **not** the cure. A bare **ASID** difference is still not a finding |
| `[TTBR SAVE-MISMATCH]` or `[TTBR LOAD-MISMATCH]` in the log | A thread was caught being switched out under (or resumed into) page tables that are not its own — the §2f corruption at the moment it happens, with both tids: [`runbooks/debug-thread-spawn-segv.md`](runbooks/debug-thread-spawn-segv.md) §2g |
| A stale waker / cross-core kill racing thread state; a killed thread runs again; a fresh clone child starts with a foreign context | The wake/publish/resume transitions on `THREAD_STATES` are CAS-guarded as of 2026-08-06 — a wake can only do WAITING→READY and nothing overwrites TERMINATED: [`runbooks/debug-thread-spawn-segv.md`](runbooks/debug-thread-spawn-segv.md) §2g. Regress with `threading::state_transition_guard_tests` (host) and `wake_transition_guards` (boot suite) |
| A wake reaches the wrong thread after a slot recycle; a wait-queue entry outlives its thread; "tids have no generations" | They do now: `SLOT_GEN` + `WakeHandle` (gen-tagged wake targets, refused when stale before any side effect). Every kernel wait queue (futex, pipe, msgqueue, eventfd, vfork, process channels) stores handles minted at enqueue, not bare tids: [`runbooks/debug-thread-spawn-segv.md`](runbooks/debug-thread-spawn-segv.md) §2g "Tid generations" |
| `SIGSEGV in clone_thread` on a process that is **not** a pthread | The line tests `proc.address_space.is_shared()`, and `vfork_process` shares the parent's L0 too — so vfork-fastpath children print it as well. Split the population before pooling evidence: [`runbooks/debug-thread-spawn-segv.md`](runbooks/debug-thread-spawn-segv.md) §0a |
| Two `SIGSEGV in clone_thread` faults in **different pids** with byte-identical `x19`/`SP_EL0`/`FAR` | Explained by the 2026-08-06 tid-flag fix (§2e), not by aliasing: the corruption was deterministic because the kernel wrote the same wrong word on every spawn. Both the determinism and the aliasing readings in [`runbooks/debug-thread-spawn-segv.md`](runbooks/debug-thread-spawn-segv.md) §2d were wrong about the mechanism |
| A userspace process wedges with every thread parked in `futex` and `pthread_create` never returning | Check for the §2e regression first: a leaked `tl_lock_count` in musl means `__tl_unlock` only decrements and **never releases**, so every later pthread call in that process blocks forever. Run `tidflags`. Otherwise: [`runbooks/debug-futex-lost-wakeup.md`](runbooks/debug-futex-lost-wakeup.md) |
| A syscall "works" but userspace misbehaves in a way no argument explains | Check whether the kernel does **more** than the flags asked for. The 2026-08-06 `clone_thread` bug was an unconditional write that Linux performs only under a specific flag — writing an unrequested output pointer is indistinguishable from memory corruption from userspace's side: [`runbooks/debug-thread-spawn-segv.md`](runbooks/debug-thread-spawn-segv.md) §2e |
| In-guest `cargo` reports `Failed to connect to index.crates.io:443` while `curl` gets `200` from the same shell | **Fixed 2026-08-20.** `sys_pselect6` never wrote the caller's `exceptfds` set, so it came back with every fd still flagged. The nightly toolchain's libcurl compiles `Curl_poll()`'s `select()` branch (curl-sys defines `HAVE_POLL_H`/`HAVE_POLL_FINE`, not plain `HAVE_POLL`) and asks for `POLLPRI` on a connecting socket, so it read the stale set back as `POLLPRI`, mapped it to `CURL_CSELECT_ERR`, and discarded every connection ~1 RTT *after* it reached `Established`. `poll(2)` callers — apk cargo, `/bin/curl` — were never affected, which is what made it look libcurl-specific. Nightly is safe as the bootstrap default again. [`runbooks/cargo-cannot-reach-crates-io.md`](runbooks/cargo-cannot-reach-crates-io.md) |
| A freshly-exec'd binary dies instantly at `ELR = N × 0x30000000 + 0x6c964`, N climbing by 1 per fault across the boot | **ROOT-CAUSED AND FIXED 2026-08-06 — same bug as the `AS MISMATCH` row below.** `entry_point_trampoline` resolved its `Process` with a `find_process(|p| p.thread_id == Some(tid))` scan; `thread_id` is a recorded slot number several teardown paths deliberately leave set, and `find_process` returns the *first ACTIVE slot*, so one stale process captures a slot and wins it back for every future occupant. The thread then runs **that** process: `run()` activates its address space and erets to its image entry point — ld-musl's `_dlstart` for a dynamic image — re-running musl's RELR `*slot += base` loop over an already-relocated interpreter page. Now resolved via `THREAD_PID_MAP` (`resolve_thread_process`), with `[TRAMP-MISMATCH]`/`[RUN-REFUSED]` tripwires. A/B: 27 faults → 0, with the race still firing 15×. [`runbooks/debug-thread-spawn-segv.md`](runbooks/debug-thread-spawn-segv.md) §2h. Regress with `test_trampoline_resolves_via_thread_pid_map` |
| `[NO-TRAPFRAME] refusing child of tid=…`, or `[syscall] clone: fork failed: No saved context` | `get_saved_user_context`'s non-trap-frame fallback is gone: it returned a process's *initial exec* entry/SP to fork/vfork/clone, which is never a valid fork return. **Hardening, not the `N × INTERP_BASE` cause** — it has never fired in an instrumented run. If you see this line it is a different defect and the missing trap frame is itself the finding: [`runbooks/debug-thread-spawn-segv.md`](runbooks/debug-thread-spawn-segv.md) §2h |
| A post-fix `-j4` run shows **zero** faults of the row above and you want to call it fixed | Don't, unless it passed **T733** with the build running. Fault onset tracks build start, not uptime; three instrumented runs wedged at T64–T132 (400 % CPU, `[BKL] stuck`) — all *before* the window where faults appear. Check `grep -ac '\[RELR\] slot'` **and** the last `[T…]` timestamp: [`runbooks/debug-thread-spawn-segv.md`](runbooks/debug-thread-spawn-segv.md) → "Verification is currently blocked by the wedge" |
| A recycled thread slot reports the previous occupant's user entry point / SP | **FIXED 2026-08-06** — `init_thread_slot_context` set `is_user_process = 1` while leaving `user_entry`/`user_sp`/`user_tls` stale, and `get_saved_user_context`'s fallback gated only on that flag. Fields now reset at slot init; the fallback also requires a non-zero `user_entry`. A real latent defect, **not** a demonstrated cause of the row above (the trap-frame branch normally wins during a clone). Regress with `threading::state_transition_guard_tests::a_recycled_slot_does_not_inherit_the_previous_occupants_user_entry` (host, two-sided) |
| A `-j4` build wedges: rustc/cargo processes alive but idle, load 0.00, no faults at wedge time | **Diagnosed 2026-08-06 — downstream of the row above, NOT a scheduler/futex bug.** The instruction aborts kill build children; cargo's jobserver tokens die with them; everyone parks forever on the empty jobserver pipe (`[PIPE-DUMP] bytes=0, writers>0` = kernel exonerated). Fix the row above; do not re-chase futexes |
| Nightly `cargo` dies instantly under HVF with `[Exception] Unknown from EL0: EC=0x0` at a **constant** `ELR`, ~71 syscalls into startup | **FIXED 2026-08-06.** The constant `ELR=0x112ac280` is OpenSSL's `OPENSSL_cpuid_setup` executing `SM3SS1` (FEAT_SM3), which Apple Silicon lacks — a CPU-feature *probe* that expects SIGILL. The kernel's `EC=0x0` arm hard-killed instead of delivering SIGILL, so the probe handler never recovered (TCG `-cpu max` implements the insn, so `HVF=0` avoided it). Fix: deliver SIGILL via `try_deliver_signal` in the `EC=0x0` arm. `EC=0x0` is an undefined instruction, **not** the `CNTP` trap the old note claimed (that would be `EC=0x18`). Nightly cargo now runs, restoring the fingerprint-cache fast path: [`runbooks/selfhost-kernel-build.md`](runbooks/selfhost-kernel-build.md) §6 + [`archive/NIGHTLY_CARGO_HVF_SIGILL.md`](archive/NIGHTLY_CARGO_HVF_SIGILL.md) |
| In-VM build sits on `Compiling akuma v0.0.7` forever at `-j4`; waiters on `0x300c2340` with `queued_for` > 900 s and **no** `[kill]`/`[Fault]` lines in the whole boot | **FIXED 2026-08-05** — and it was not a futex bug at all. `[FUTEX-ORPHAN] hist=...EpW` (woken, never unparked) means the wake reached the scheduler and died there: `schedule_blocking` published `WAITING` before re-reading the sticky `WOKEN_STATES` flag, so a waker landing in the gap dropped the wake permanently. [`runbooks/debug-futex-lost-wakeup.md`](runbooks/debug-futex-lost-wakeup.md) §4a + [`reference/subsystems/scheduler.md`](reference/subsystems/scheduler.md) -> "Park/wake handshake". `-j1` is still the documented recipe until a `-j4` run confirms the fix in-VM |
| A script exits **127** with `<scriptname>: applet not found`, or `apk` ends in `1 error` naming a `.post-upgrade`/`.trigger` script | **Not a missing applet — a kernel `execve` bug, and it breaks every `#!/bin/sh` script.** `exec_shebang` (`src/syscall/proc.rs`) puts the *symlink-resolved* interpreter in `argv[0]`, so `#!/bin/sh` (a symlink to busybox) runs busybox as `busybox <script>`; busybox is a multi-call binary that dispatches on `argv[0]`, so it looks the script path up as an applet name. Linux passes the interpreter **as written** and resolves only to load. `spawn.rs::resolve_shebang_chain` already does it right — the two implementations disagree. Diagnosis + the 5-invocation proof: [`archive/MINIMAL_DEV_BUSYBOX_APPLETS.md`](archive/MINIMAL_DEV_BUSYBOX_APPLETS.md) §V4; symptom history: [`archive/DEVBOX_ISSUES.md`](archive/DEVBOX_ISSUES.md) Issue 8 |
| A boot marker never appears in the console log though the VM is healthy (harness reports `booted: False` / `BOOT_FAIL`, but herd and sshd are up) | **Cross-core console interleaving tore the line in half** at `SMP>1` — `[herd] Started sshd (pid= 2)` split so neither `Started sshd` nor `sshd started` matches. Gate on an ssh round-trip, never a console string. [`archive/DEVBOX_ISSUES.md`](archive/DEVBOX_ISSUES.md) Issue 3 (its `FIXED` line is wrong) + [`runbooks/verify-trim-fat-change.md`](runbooks/verify-trim-fat-change.md) § "Before calling anything a regression" item 4 |
| `Exec format error (os error 8)` on the same `build-script-build` on every retry | **Not a kernel bug** — a 0-byte artifact from a crashed link that cargo still considers fresh. Delete the fingerprint dir: [`runbooks/selfhost-kernel-build.md`](runbooks/selfhost-kernel-build.md) §5.4 |
| `rust-lld` rejects a `.rlib` it just built — `invalid sh_name (0x5000feed)`, `Archive::children failed: truncated or malformed archive`, `invalid sh_type … expected SHT_STRTAB` | **Read the bytes: `0xfeed`/`0xFEEDFACE` is `akuma_pmm` quarantine poison.** A frame backing a file-backed mapping was freed while the mapper still had it mapped, so the linker read poison as file content. Was the dominant self-host build failure (~1 green in 3–4 clean builds, same on the as-found control); `[FILL-SHORT]`/`[E2-EOF]`/`[PMM-POISON]` all read 0 — the kernel never notices. The poison names its own frame (`^ 0xFEEDFACEDEAD0000`). **FIXED 2026-08-15** (cache refcount races W1/W2; verified 6/10 red → 10/10 green): [`archive/MAPPED_PAGE_PREMATURE_FREE_FIX.md`](archive/MAPPED_PAGE_PREMATURE_FREE_FIX.md). `[PMM-RESURRECT]` must never print. The free pipeline and windows W1–W6: [`reference/subsystems/memory.md`](reference/subsystems/memory.md) § "Frame lifecycle: the free pipeline". Regression probes: `fpcprobe` / `shareprobe` |
| A file-backed mapping reads **zeros** where file content belongs, and the console shows `[FILL-SHORT] inode=0 … got=Err(NotFound) — page left zero-filled` | The fd layer is path-identified (`KernelFile` has no inode), so an mmap racing a rename records `inode=0` and a fault in a later rename window zero-fills instead of reading the file. OPEN — found by `fpcprobe` 2026-08-15; write-tmp-then-rename (cargo's atomic-write pattern) is the natural trigger. Real fix = inode identity pinned at `open()`. See [`archive/MAPPED_PAGE_PREMATURE_FREE_FIX.md`](archive/MAPPED_PAGE_PREMATURE_FREE_FIX.md) § "Targeted probes" |
| `cargo build` of a big third-party workspace fails on the first registry deps with `could not execute process \`rustc …\` (never executed) / Bad address (os error 14)`, while the same cargo builds `/tmp/akuma` and a one-dep hello crate fine, and running the identical rustc argv by hand works | **OPEN (2026-08-17).** Not argv/envp size, not `env_clear`, not `current_dir`, not threaded spawn (all mimicked clean), and **not a kernel-returned EFAULT** — with `SYSCALL_ERRNO_DIAG_ENABLED` narrowed to EFAULT, the kernel logs zero EFAULTs while cargo reports three, and no `[syscall] execve` line appears for the failing spawns: the errno is born pre-execve (child-side chdir/dup2/CLOEXEC-pipe) or in userspace. Full bisection + what was ruled out: [`archive/NCA_MISSING_SYSCALLS.md`](archive/NCA_MISSING_SYSCALLS.md) §1 |
| A Rust binary using `UnixListener`/`UnixStream` (e.g. `nca`'s session IPC) dies at startup with `Connection failed: Address family not supported by protocol (os error 97)` | `sys_socket` accepts only `AF_INET` (domain 2); AF_UNIX exists solely as `socketpair` (syscall 199). Patched 2026-08-17 directly in the `native-cli-ai` fork (bind failure degrades to IPC-less mode instead of a fatal error). Real fix = named AF_UNIX stream sockets: [`archive/NCA_MISSING_SYSCALLS.md`](archive/NCA_MISSING_SYSCALLS.md) §2 |
| A guest TCP client hangs forever reading a response whose **first byte is delayed** >~5 s (e.g. nca↔Ollama with a slow-prefill model), while instant responses on the same path stream fine — and the identical request through a host-side proxy that answers chunks promptly works | **OPEN (2026-08-17), kernel suspicion: lost wakeup of a blocked socket reader when data arrives after a long idle window.** Ruled out: SLIRP path (proxy run traverses it too), keep-alive reuse, non-blocking writes. Verified with nca↔Ollama: qwen3.5:0.8b round trips OK, gemma4:e4b prefill hangs. [`archive/SOCKET_DELAYED_FIRST_BYTE_HANG.md`](archive/SOCKET_DELAYED_FIRST_BYTE_HANG.md) + DEVBOX_ISSUES Issue 17; compact version + nca context: [`archive/NCA_MISSING_SYSCALLS.md`](archive/NCA_MISSING_SYSCALLS.md) §2b |
| A Rust/tokio binary finishes its work correctly and then aborts **on exit** with `thread 'tokio-rt-worker' panicked at .../tokio-*/src/fs/file.rs:745: called \`Option::unwrap()\` on a \`None\` value` | **Not a syscall gap and not an Akuma bug** — upstream tokio leaves `File` in `State::Idle(None)` when `spawn_mandatory_blocking` returns `None` during runtime shutdown (the `?` at file.rs:767 returns without restoring the buffer taken at :745), so the *next* `poll_write` unwraps `None`. Akuma only makes it likely, because slow ext2 writes leave appends in flight at teardown. Tell: the caller writes twice per record and `let _ =`-discards both results, so the first write is poisoned silently and the second aborts. [`archive/NCA_MISSING_SYSCALLS.md`](archive/NCA_MISSING_SYSCALLS.md) §7 |
| Boot on a long-lived `devbox.img` degrades to **15+ minutes** behind a ~1900-line watchdog storm, after dozens of build cycles and hard kills | **Not a kernel regression — the image is damaged.** A campaign of `cargo clean` + full builds with repeated `kill -9` accumulates unattached inodes and wrong free-block counts; the filesystem never reports an error, it just gets slow. Repair from a container with `e2fsprogs` (`e2fsck -fy -D`) and **re-run until it exits 0**: [`runbooks/selfhost-kernel-build.md`](runbooks/selfhost-kernel-build.md) §5.5. Any measurement taken before the repair is uninterpretable ([`archive/SELFHOST_ZERO_PAGE_HUNT.md`](archive/SELFHOST_ZERO_PAGE_HUNT.md) §14) |
| `qemu-system-aarch64: Assertion failed: (isv), … hvf.c, line 1883` (QEMU exits 134) | HVF could not decode a guest MMIO access — the kernel dereferenced garbage into the MMIO window. Downstream symptom of the ld-musl class above; a supervisor loop must treat it as a normal outcome and reboot |
| AB-BA deadlock hunting / "can this `Spinlock` just be an atomic?" | [`reference/subsystems/locking.md`](reference/subsystems/locking.md) -> gate 2 + [`archive/BKL_FINE_GRAINED_LOCKING_PLAN.md`](archive/BKL_FINE_GRAINED_LOCKING_PLAN.md) §7.3a (phase 7g classification table) |
| A container cannot run commands its image obviously ships — `sh: cat: Permission denied` — while the same binary by explicit path works | The binaries lost `+x` during extraction and a shell's `PATH` search calls `access(X_OK)`; nothing checks the exec bit at *spawn*, which is why the two spellings disagree. Root cause was `/bin/tar` being a busybox applet plus `sys_linkat` copying whole files. **FIXED 2026-08-11**: [`archive/BOX_DOCKER_COMPAT.md`](archive/BOX_DOCKER_COMPAT.md). Tell: a layer store far larger than the image (`du -sh /var/lib/box/layers/*/`) |
| `mount` inside a box returns EPERM where it used to work | Intended since 2026-08-11: a box's mount namespace is composed from outside, by box 0, before it runs. Consequence: no nested OCI images. [`reference/subsystems/containers.md`](reference/subsystems/containers.md) -> "Mount policy" |
| An SSH session closes as if you typed `exit` whenever a `box run`/`box open`/`box use` child finishes | **FIXED 2026-08-11.** `spawn` registered the per-tid channel by re-reading `p.channel`, which `reattach` had already pointed at the caller's channel — so the child's `sys_exit` stamped the *shell's* channel and sshd ended the session. [`archive/BOX_DOCKER_COMPAT.md`](archive/BOX_DOCKER_COMPAT.md) -> "The session-closing bug" |
| `box grab <name> <pid>` reattaches (no error), the target's own output may even stream through, but anything you type is never delivered — the process doesn't respond and doesn't exit either | **FIXED 2026-08-23.** `sys_read`'s stdin loop and `sys_poll_input_event` each cached the process's `Arc<ProcessChannel>` once before blocking and kept reusing it across the whole wait; `reattach` swaps in a *new* channel, which an already-parked read never saw — the wake fired correctly, it just kept draining the abandoned old channel. [`archive/REATTACH_STALE_CHANNEL_HANG.md`](archive/REATTACH_STALE_CHANNEL_HANG.md) |
| `box grab` sits forever even after the process it grabbed has plainly exited (or a second, unrelated `box grab` on the same pid silently steals the session with no warning) | **FIXED 2026-08-23.** Two separate gaps: (1) `waitpid()` can only ever report a real *child's* exit, and the grabbed pid essentially never is one — `box grab` now falls back to a `kill(pid, 0)` liveness probe. (2) `sys_reattach` gained a `force` arg (`box grab -d`, `screen -d`-style): without it, reattaching to an already-held target is refused (`EBUSY`) instead of silently stealing it; with it, the previous holder is `SIGTERM`'d so its own wait loop actually exits. [`archive/REATTACH_STALE_CHANNEL_HANG.md`](archive/REATTACH_STALE_CHANNEL_HANG.md) §6 |
| `box run: failed to spawn /entrypoint.sh` | `spawn_ext` (315) does not honour shebangs; only `execve` does. Use `--entrypoint <binary>`: [`runbooks/run-docker-image.md`](runbooks/run-docker-image.md) |
| A boxed process reads/writes files outside its `box_root`, kills another box, spawns into one, or repoints another box's network stack — or a box syscall now returns `EPERM` where it used to work | Box permissions were **advisory until 2026-08-08**: `box_mod::access`'s rules were unit-tested but had no callers, and `parent_box_id` was hardcoded `None` so every ancestry check was blind. Nine boundaries now enforced at the syscall boundary: [`reference/subsystems/containers.md`](reference/subsystems/containers.md) -> "Box permissions" + [`archive/BOX_ISOLATION_SECURITY_FIXES.md`](archive/BOX_ISOLATION_SECURITY_FIXES.md). Regress with `test_box_isolation_syscall_guards` |
| `userspace/build.sh` stops partway with `error: package ID specification 'meow' did not match any packages`, or dies at the link step with `rust-lld: error: cannot find linker script linker.ld` | The four submodule-backed members (`meow`, `tcc`, `llama-cpp`, `nca`) are **not** in the userspace workspace, so `cargo build -p <name>` cannot see them and the repo-root `-Tlinker.ld` rustflag (a relative path) does not resolve from their own workspace roots. **FIXED 2026-08-12**: [`archive/USERSPACE_BUILD_SH_OUT_OF_WORKSPACE_MEMBERS.md`](archive/USERSPACE_BUILD_SH_OUT_OF_WORKSPACE_MEMBERS.md) |
| A herd service that segfaults respawns instantly forever, `restart_count` pinned at 0, never reaching `Failed`; `herd status` reports the crash as `code 0`; `herd stop <svc>` leaves the process running and a second copy appears | `waitpid` returns `WEXITSTATUS` only, so a signal death decoded as `exit 0`, and `libakuma::kill` hardcodes signal 0 (an existence probe the kernel never delivers). **FIXED** (2026-08-08): [`../userspace/herd/docs/SIGNAL_EXIT_HANDLING.md`](../userspace/herd/docs/SIGNAL_EXIT_HANDLING.md). Note busybox `sh` catches SIGTERM and exits 130, so `kill -15` is *not* a way to test the signal path |

## Task list - "I want to do X"

| Task | Start here |
|---|---|
| Boot a VM and connect via SSH | [`runbooks/boot-and-connect.md`](runbooks/boot-and-connect.md) |
| Understand why `--profile X` alone gives a different kernel than `scripts/build_X.sh` | [`archive/TRIM_FAT_PROFILES_AND_ACCEPTANCE.md`](archive/TRIM_FAT_PROFILES_AND_ACCEPTANCE.md) § A |
| Build the devbox and SSH in | [`runbooks/build-devbox.md`](runbooks/build-devbox.md) |
| Compile the kernel inside Akuma (self-host) | [`runbooks/selfhost-kernel-build.md`](runbooks/selfhost-kernel-build.md) — **stable as of 2026-08-16: 20/20 clean builds first try**, 10 each on two different kernels, every PMM tripwire silent, **~2 min per trial** (not the ~7–12 min long on record). The retry-through-crashes era is over — a failure is a finding, not noise. A meaningful trial starts with `cargo clean` (no script does it for you), and `--offline` needs the registry primed once with `cargo fetch`. Read **How long a trial takes** and **Give every trial its own sentinel** before writing any batch harness |
| Compile `userspace/build.sh` members inside Akuma (self-host, not the kernel) | [`userspace/SELF_HOSTING_USERSPACE.md`](userspace/SELF_HOSTING_USERSPACE.md) — needs bash + the nightly toolchain at `/usr/local` on `PATH`, plus an apk-cargo `fetch` first (nightly cargo's networking is broken — already root-caused). The cold-build cargo heap-corruption class (~25 % rate, "retry, don't debug") was **root-caused and FIXED 2026-08-15** — cache refcount windows W1/W2, [`archive/MAPPED_PAGE_PREMATURE_FREE_FIX.md`](archive/MAPPED_PAGE_PREMATURE_FREE_FIX.md); the audit trail is [`archive/CARGO_NULL_RC_MEMORY_REFERENCE_AUDIT.md`](archive/CARGO_NULL_RC_MEMORY_REFERENCE_AUDIT.md) |
| Build the `extreme-size` image, or shrink an image that is running out of memory | [`runbooks/build-extreme-size.md`](runbooks/build-extreme-size.md) — includes reverting sshd to one process for all sessions |
| Understand how sshd serves concurrent connections, or test it under load | [`userspace/sshd/docs/PROCESS_PER_SESSION.md`](../userspace/sshd/docs/PROCESS_PER_SESSION.md) + `scripts/sshd_concurrency_test.py` |
| Run a Docker image (`box pull` / `box run`) | [`runbooks/run-docker-image.md`](runbooks/run-docker-image.md) |
| Run Redis — the Alpine package, or the official `redis:alpine` image in a box, reachable from the host | [`runbooks/run-redis.md`](runbooks/run-redis.md) — working end to end since 2026-08-16; the *why* is [`archive/REDIS_END_TO_END.md`](archive/REDIS_END_TO_END.md). Use guest port **4444** (6379 is not forwarded), and `--entrypoint` until per-process credentials exist |
| Work on `box` or `herd`, or wonder why both restate the box ABI | [`archive/TRIM_FAT_HERD_PLUS_BOX.md`](archive/TRIM_FAT_HERD_PLUS_BOX.md) — herd depends on `boxlib` (`SpawnOptions`, syscall numbers, `box_id_for`, JSON reads all shared); implemented 2026-08-12 |
| Add an apk package to the devbox | [`runbooks/add-apk-package.md`](runbooks/add-apk-package.md) |
| Add a kernel `sc-*` feature | [`runbooks/add-syscall-feature.md`](runbooks/add-syscall-feature.md) |
| Recover a wedged/hung VM | [`runbooks/recover-wedged-vm.md`](runbooks/recover-wedged-vm.md) |
| Merge duplicated code (a `TRIM_FAT_EMBARASSING_DUPLICATIONS.md` phase item) and prove it changed nothing | [`runbooks/verify-trim-fat-change.md`](runbooks/verify-trim-fat-change.md) — run [`scripts/verify_trim.py`](../scripts/verify_trim.py) and diff against your parent commit; the runbook says which log lines are known-benign, how to decide host test vs boot test, and (Tier 4) the redis memtest for memory-path changes |
| Update `docs/archive/BUG_FIX_LIST.md` after landing a fix | [`runbooks/update-bug-fix-list.md`](runbooks/update-bug-fix-list.md) |
| Export an HTML slide deck under `bootstrap/public/` to PDF | [`runbooks/print-deck-to-pdf.md`](runbooks/print-deck-to-pdf.md) — run [`scripts/render_deck_pdf.py`](../scripts/render_deck_pdf.py), **not** Cmd-P or `chrome --print-to-pdf`: Chrome lays print media out at a ~800px viewport whatever `@page` says, so the two-column slides paginate as the mobile stack with the `vw` type scale magnified |
| Run Akuma under Firecracker on real hardware (AWS bare metal), or get the root image onto a remote host | [`overlays/devbox-firecracker-aws/README.md`](../overlays/devbox-firecracker-aws/README.md) for the packaging; the infrastructure is the separate `akuma-terraform` repo, written up in [`archive/AKUMA_FIRECRACKER_TERRAFORM.md`](archive/AKUMA_FIRECRACKER_TERRAFORM.md). `.metal` is not a preference — it is the only EC2 family that exposes `/dev/kvm` |
| Find out what a Firecracker microVM's machine *actually* looks like, rather than what its source said | [`runbooks/dump-firecracker-fdt.md`](runbooks/dump-firecracker-fdt.md) — boots a Linux guest that exposes `/sys/firmware/fdt`. The measured blobs for 1/2/4/8 vCPUs are in [`reference/firecracker/fdt/`](reference/firecracker/fdt/) |
| Toggle debug knobs | [`reference/subsystems/config-flags.md`](reference/subsystems/config-flags.md) |
| Run the acceptance playbooks | [`acceptance/`](../acceptance/) |

## Subsystem reference index

| Subsystem | Doc | Grade |
|---|---|---|
| Boot / MMU / DTB | [`reference/subsystems/boot.md`](reference/subsystems/boot.md) | B |
| Memory (PMM, heap, COW) | [`reference/subsystems/memory.md`](reference/subsystems/memory.md) | **C** |
| Scheduler / threads | [`reference/subsystems/scheduler.md`](reference/subsystems/scheduler.md) | A |
| Thread & thread-group lifecycle (states, teardown lock tree, eret leaves) | [`reference/subsystems/thread-lifecycle.md`](reference/subsystems/thread-lifecycle.md) | **C** |
| SMP (shared-kernel) | [`reference/subsystems/smp-shared.md`](reference/subsystems/smp-shared.md) | **C** |
| Kernel locking (BKL + carve-outs) | [`reference/subsystems/locking.md`](reference/subsystems/locking.md) | B |
| Networking (box model, two stacks) | [`reference/subsystems/networking.md`](reference/subsystems/networking.md) | **C** |
| Rump stack (sysproxy, fiber) | [`reference/subsystems/rump-stack.md`](reference/subsystems/rump-stack.md) | **C** |
| SSH (built-in + userspace) | [`reference/subsystems/ssh.md`](reference/subsystems/ssh.md) | A |
| VFS (ext2, procfs, pipes) | [`reference/subsystems/vfs.md`](reference/subsystems/vfs.md) | A |
| Syscalls / Linux ABI | [`reference/subsystems/syscalls.md`](reference/subsystems/syscalls.md) | A |
| Containers / boxes / herd | [`reference/subsystems/containers.md`](reference/subsystems/containers.md) | B |
| Cargo features + env knobs | [`reference/subsystems/config-flags.md`](reference/subsystems/config-flags.md) | — |
| Build profiles / distributions (release, size, extreme-size, devbox, release-smp) | [`reference/build-profiles.md`](reference/build-profiles.md) | — |
| IRQ / console / RNG / async-fs | [`reference/subsystems/irq.md`](reference/subsystems/irq.md) etc. | B / B / B / A |
| In-kernel shell / editor | **REMOVED 2026-08-10** — the reference docs went with them. History: [`archive/IN_KERNEL_SHELL.md`](archive/IN_KERNEL_SHELL.md) / [`archive/IN_KERNEL_EDITOR_NEKO.md`](archive/IN_KERNEL_EDITOR_NEKO.md), rationale in [`archive/BUILTIN_SSH_REMOVAL.md`](archive/BUILTIN_SSH_REMOVAL.md). Userspace replacements: `/bin/paws` or busybox `sh`, and `/bin/vi` | — |
| Drivers (GIC, timers, block, fw_cfg) | [`reference/subsystems/drivers/`](reference/subsystems/drivers/) | B / B / A / A |
| Exceptions (vector table, trap frame, ESR_EL1) | [`reference/subsystems/exceptions.md`](reference/subsystems/exceptions.md) | **C** |
| `akuma-primitives` (the dependency-free leaf: `OnceCopy`, `safe_print!`, all DAIF, `PreemptGuard`, phys/virt) | [`reference/subsystems/primitives.md`](reference/subsystems/primitives.md) | A |
| `akuma-firecracker` (the machine description read from the FDT at run time: RAM, GIC distributor/redistributor, console, virtio slots) | crate docs — `crates/akuma-firecracker/src/lib.rs`. Measured fixtures from **both** machines: [`reference/firecracker/fdt/`](reference/firecracker/fdt/) (Firecracker) and `crates/akuma-firecracker/fixtures/` (QEMU virt). Not yet wired into the kernel; `src/platform.rs` still holds the literals | A |

Syscalls / Linux ABI now has 17 per-family docs under
[`reference/subsystems/syscalls/`](reference/subsystems/syscalls/) — grades
vary per family; `mem`, `net`, `signal`, `sync` are **C** (active risk,
touched in the Jun 2026 crisis).

**Still undocumented (deferred gap list):** audio (`src/audio.rs`) and the
framebuffer device itself (`src/ramfb.rs` — distinct from the `fb.rs` syscall
wrapper, which is documented), both Tier C / niche. The full gap list is in
[`reference/README.md`](reference/README.md) -> Not yet written.

## When in doubt

If a runbook or reference doc doesn't cover your problem, search `archive/` —
the historical investigations are thorough even if not action-oriented. Every
new doc has a "Background" footer linking back to the relevant archive originals.
